---
phase: quick-260818-rj2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - ISSUE-36
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "README.md has a `## Examples` section placed immediately before the `## Documentation` section."
    - "The Examples section links to all 6 runnable examples/ scripts, each with its verbatim one-line description from examples/README.md."
    - "The Examples section links to all 11 docs/examples/ cookbook pages."
    - "The section notes that every example shows the expected input DataFrame shape (addresses issue #36)."
  artifacts:
    - README.md
  key_links:
    - "Relative markdown links resolve from repo root: [text](examples/01_ols_regression.py) and [text](docs/examples/ab-testing.md)."
---

<objective>
Add a new `## Examples` section to `README.md` that makes the existing runnable
`examples/` scripts and `docs/examples/` cookbook pages discoverable from the main
README. Fixes GitHub issue #36 — a non-statistician user could not tell what the
input Polars DataFrame should look like for each method, and could not find the
example files that already answer this.

Purpose: The examples already exist but are invisible from the main README (which
only references `examples/rust_wls.rs`). Surfacing them directly addresses the
user's core concern about expected input DataFrame shapes.

Output: An updated `README.md` with one new `## Examples` section inserted before
the existing `## Documentation` section. No other file touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

# Target file — the new section goes immediately BEFORE `## Documentation` (~line 354).
@README.md

# Source of verbatim example descriptions (Regression / Statistical Tests / Forecasting tables).
@examples/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ## Examples section to README.md linking runnable scripts and cookbook pages</name>
  <files>README.md</files>
  <action>
Insert a new `## Examples` section into README.md immediately BEFORE the existing
`## Documentation` heading (currently around line 354). Do NOT restructure, reorder,
or edit any existing section — this is a pure insertion of one new section.

The new section must contain three parts, using Markdown tables consistent with the
existing README style (`| Example | Description |` header rows and `[text](path)`
relative links, matching the tables already in examples/README.md and README.md):

1. A short intro sentence for the section noting that every example shows the
   expected input DataFrame shape — this directly addresses issue #36 (a
   non-statistician could not tell what the input DataFrame should look like).

2. A "Runnable examples" table linking the 6 files in `examples/`, each with its
   ONE-LINE description pulled VERBATIM from examples/README.md. Use these exact
   relative paths and descriptions (copy descriptions character-for-character from
   examples/README.md lines 19-33):
   - [examples/01_ols_regression.py](examples/01_ols_regression.py) — "OLS regression basics: fitting, coefficients, predictions, and R-style formulas"
   - [examples/02_grouped_regression.py](examples/02_grouped_regression.py) — "Running regression per group with `group_by` and `over`"
   - [examples/03_glm_models.py](examples/03_glm_models.py) — "Generalized Linear Models: logistic (binary), Poisson (counts)"
   - [examples/04_statistical_tests.py](examples/04_statistical_tests.py) — "T-tests, Mann-Whitney U, Shapiro-Wilk, and other hypothesis tests"
   - [examples/05_demand_classification.py](examples/05_demand_classification.py) — "AID (Automatic Identification of Demand) for demand pattern classification"
   - [examples/rust_wls.rs](examples/rust_wls.rs) — a short description such as "Rust API example: Weighted Least Squares via `wls_fit`" (rust_wls.rs is NOT in examples/README.md's tables, so write a brief accurate one-liner for it; it is already referenced elsewhere in README so keep the wording consistent).

3. A "Cookbook (docs/examples/)" table linking all 11 pages in docs/examples/ with
   readable titles. Use these exact relative paths:
   [docs/examples/ab-testing.md](docs/examples/ab-testing.md),
   [docs/examples/advanced-correlation.md](docs/examples/advanced-correlation.md),
   [docs/examples/categorical-analysis.md](docs/examples/categorical-analysis.md),
   [docs/examples/equivalence-testing.md](docs/examples/equivalence-testing.md),
   [docs/examples/forecast-comparison.md](docs/examples/forecast-comparison.md),
   [docs/examples/glm-models.md](docs/examples/glm-models.md),
   [docs/examples/group-analysis.md](docs/examples/group-analysis.md),
   [docs/examples/hypothesis-testing.md](docs/examples/hypothesis-testing.md),
   [docs/examples/regression-workflow.md](docs/examples/regression-workflow.md),
   [docs/examples/regularized-regression.md](docs/examples/regularized-regression.md),
   [docs/examples/special-models.md](docs/examples/special-models.md).
   Derive human-readable titles from the filenames (e.g. "A/B testing",
   "Advanced correlation", "Categorical analysis", "Equivalence testing (TOST)",
   "Forecast comparison", "GLM models", "Group analysis", "Hypothesis testing",
   "Regression workflow", "Regularized regression", "Special models").

Use the Edit tool with a scoped replacement anchored on the existing
`## Documentation` heading line (insert the new section + a blank line before it).
Do NOT touch CHANGELOG.md, Cargo.toml, pyproject.toml, or any code/test file — those
carry unrelated pre-existing uncommitted changes that must be left untouched.
  </action>
  <verify>
    <automated>bash -c 'grep -q "^## Examples$" README.md && awk "/^## Examples$/{e=NR} /^## Documentation$/{d=NR} END{exit !(e>0 && d>0 && e<d)}" README.md && for p in examples/01_ols_regression.py examples/02_grouped_regression.py examples/03_glm_models.py examples/04_statistical_tests.py examples/05_demand_classification.py docs/examples/ab-testing.md docs/examples/special-models.md docs/examples/regularized-regression.md; do grep -qF "($p)" README.md || { echo "MISSING link: $p"; exit 1; }; done && echo OK'</automated>
  </verify>
  <done>
README.md contains a `## Examples` heading located before `## Documentation`; the
section links all 6 examples/ scripts (with verbatim descriptions for the 5 Python
ones) and all 11 docs/examples/ cookbook pages; the section notes that examples show
the expected input DataFrame shape. No other file is modified; the grep/awk verify
command prints OK.
  </done>
</task>

</tasks>

<verification>
- `grep -c "^## Examples$" README.md` returns 1.
- The `## Examples` heading appears before the `## Documentation` heading (awk line-order check).
- All 6 `examples/` links and all 11 `docs/examples/` links are present in README.md.
- `git status --porcelain README.md` shows README.md as the only newly-modified doc from this task (CHANGELOG.md, Cargo.toml, pyproject.toml remain as they were — not staged by this task).
</verification>

<success_criteria>
Issue #36 is addressed: a reader of the main README can discover both the runnable
`examples/` scripts (with descriptions) and the `docs/examples/` cookbook pages, and
is told the examples demonstrate the expected input DataFrame shape — all without
restructuring any existing README section or touching non-README files.
</success_criteria>

<output>
Create `.planning/quick/260818-rj2-add-examples-section-to-readme-linking-r/260818-rj2-SUMMARY.md` when done.
</output>
