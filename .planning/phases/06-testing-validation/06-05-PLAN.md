---
phase: 06-testing-validation
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified: []
autonomous: false
requirements: [TEST-05]
user_setup: []
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "clippy -D warnings, cargo fmt --check, and ruff all pass locally"
    - "cargo test --no-default-features --test rust_api passes"
    - "The full maturin develop + pytest suite passes (run by the phase gate / orchestrator)"
  artifacts: []
  key_links:
    - "This plan confirms all local CI gates green; the full OS/py matrix is delegated to GH Actions CI"
---

<objective>
Final green-gate plan for Phase 6: confirm every local CI gate passes after the value tests
(06-01/02/03) and Rust-side tests (06-04) land — clippy `-D warnings`, cargo fmt, ruff, and the
Rust rust_api suite are executor-runnable; the full `maturin develop && pytest` run is isolated as
a phase-gate step executed by the ORCHESTRATOR (executor subagents stall on maturin/pytest).

Purpose: Satisfy TEST-05 — make the LOCAL suite authoritative and green. The full matrix
(Python 3.9–3.12 × Linux/macOS/Windows) is validated by the existing GH Actions CI on push, not in
this sandbox.
Output: Confirmation that all local gates are green; any lint/format fixes needed to keep gates
green are applied here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-validation/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local static gates — clippy, fmt, ruff, rust_api</name>
  <files>tests/rust_api.rs, tests/conftest.py</files>
  <read_first>
    - .github/workflows/ci.yml (exact clippy/fmt/ruff invocations the CI uses — mirror them)
    - .planning/phases/06-testing-validation/06-CONTEXT.md (TEST-05 local-authoritative decision)
  </read_first>
  <action>
    Run the same static gates the CI runs, mirroring .github/workflows/ci.yml: cargo fmt --check,
    cargo clippy with -D warnings on the workspace, ruff check on python/ and tests/, and
    cargo test --no-default-features --test rust_api. If any test file added in 06-01..04 trips
    ruff (line length 100, py39 target) or clippy, apply the minimal fix in that test file to
    restore green. Do NOT run maturin/pytest here — that is Task 2 (orchestrator-run). Report the
    pass/fail of each gate.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5 && ruff check tests/ python/ && cargo test --no-default-features --test rust_api 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo fmt --check exits 0
    - cargo clippy -D warnings exits 0
    - ruff check tests/ python/ exits 0
    - cargo test --no-default-features --test rust_api passes
  </acceptance_criteria>
  <done>All static local gates (fmt, clippy, ruff) and the rust_api suite are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Orchestrator-run maturin+pytest full-suite phase gate</name>
  <what-built>The full pytest value+shape suite for all new API (06-01/02/03) plus the Rust-side
  suite (06-04). This step is the orchestrator-run phase gate: build the extension and run pytest,
  which executor subagents cannot do without stalling.</what-built>
  <how-to-verify>
    Run, from the repo root, in the FOREGROUND (maturin must not be backgrounded):
    1. `maturin develop --release` (or the project's standard develop invocation)
    2. `pytest -q` (scipy/statsmodels come from the dev extras; sklearn-guarded tests skip if absent)
    3. Confirm 0 failures. Optionally run `mkdocs build --strict` if mkdocs is installed
       (deferred DOCS-02 live check).
    Expected: the full pytest suite passes (new value assertions green; importorskip-guarded tests
    skip cleanly where the optional lib is absent), and rust_api is green.
  </how-to-verify>
  <resume-signal>Type "approved" once the full maturin+pytest suite passes, or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-dep environment | scipy/statsmodels (and optional sklearn) resolved into the dev/test env for the gate run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | test-only deps (scipy/statsmodels in dev extras; sklearn optional) | low | accept | Test-only, importorskip-guarded, never in the runtime wheel; full OS/py matrix runs on GH Actions CI |
</threat_model>

<verification>
- All local static gates green (executor-run).
- Full maturin+pytest suite green (orchestrator-run phase gate).
- Full Python/OS matrix delegated to GH Actions CI on push.
</verification>

<success_criteria>
The local suite is comprehensive and green (clippy/fmt/ruff/rust_api + full pytest), making it
authoritative for TEST-05; the CI matrix confirms it across platforms on push.
</success_criteria>

<output>
Create `.planning/phases/06-testing-validation/06-05-SUMMARY.md` when done.
</output>
