---
phase: 06-testing-validation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tests/test_statistics_parity.py
  - tests/test_correlation.py
autonomous: true
requirements: [TEST-01, TEST-03]
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 33000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "two_way_anova and repeated_measures_anova outputs are value-validated (vs scipy/statsmodels or analytic constants)"
    - "energy_distance_nd asserts the separation property (larger for separated samples than for identical samples)"
    - "icc matrix-input output is validated against a published/analytic ICC value"
  artifacts:
    - tests/test_statistics_parity.py
    - tests/test_correlation.py
  key_links:
    - "value tests reuse the require_scipy/require_statsmodels guards from conftest.py (06-01)"
---

<objective>
Upgrade the existing shape-only smoke tests for the remaining newly exposed STATISTICS symbols
(two_way_anova, repeated_measures_anova, energy_distance_nd, icc) from shape-only to
value-correctness, following the scipy/analytic reference pattern established by the 06-01 tracer.

Purpose: Satisfy TEST-01 (shape+value per new statistics symbol) and TEST-03 (validate against
references where available). one_way_anova is already covered by 06-01.
Output: Value assertions added to the existing statistics + correlation test files — no duplicate
test files, no new library symbols.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-validation/06-CONTEXT.md
@.planning/phases/02-public-api-audit/02-API-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Value-validate two_way_anova, repeated_measures_anova, energy_distance_nd</name>
  <files>tests/test_statistics_parity.py</files>
  <read_first>
    - tests/test_statistics_parity.py (existing smoke cases for these three symbols — upgrade in place)
    - tests/conftest.py (require_scipy / require_statsmodels helpers from 06-01)
    - python/polars_statistics/exprs/parametric.py (def two_way_anova, def repeated_measures_anova, def energy_distance_nd — signatures + output field names)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (ANOVA result struct fields; energy_distance nD contract)
  </read_first>
  <action>
    Upgrade the three existing cases in test_statistics_parity.py to add value assertions alongside
    the existing shape/schema checks (do not create parallel duplicate tests). For two_way_anova,
    validate the main-effect and interaction F/p against statsmodels ols+anova_lm on the same
    factorial dataset (guarded by require_statsmodels), or against a textbook two-way ANOVA table
    constant if statsmodels layout differs — document which in a docstring. For
    repeated_measures_anova, assert the within-subject F/p against a published RM-ANOVA worked
    example (analytic constants) since scipy lacks a direct RM-ANOVA; also assert the sphericity /
    Greenhouse-Geisser correction fields are present and within [0,1]. For energy_distance_nd,
    assert the separation property: the statistic on two clearly separated multivariate samples is
    strictly greater than on two identical/overlapping samples, and the p-value for well-separated
    samples is small. Use loose tolerances appropriate to each statistic. Reference output field
    names exactly as defined in parametric.py.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/test_statistics_parity.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - test_statistics_parity.py asserts two_way_anova values vs statsmodels or a documented analytic constant
    - repeated_measures_anova has a value assertion against a published example + sphericity/correction field checks
    - energy_distance_nd asserts separated-samples statistic > identical-samples statistic
    - grep -v '^#' tests/test_statistics_parity.py | grep -c 'energy_distance_nd' returns >= 1
  </acceptance_criteria>
  <done>Each of the three remaining ANOVA/energy statistics symbols has a value-correctness
  assertion (reference-lib or analytic) in addition to its existing shape check.</done>
</task>

<task type="auto">
  <name>Task 2: Value-validate matrix-input icc against a published ICC example</name>
  <files>tests/test_correlation.py</files>
  <read_first>
    - tests/test_correlation.py (existing icc smoke case — upgrade in place)
    - tests/conftest.py (skip helpers)
    - python/polars_statistics/exprs/correlation.py (def icc — matrix-input signature, ICCType parameter, output fields)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (ICC stub → real matrix-input contract; ICCType variants)
  </read_first>
  <action>
    Upgrade the existing icc case in test_correlation.py to a value test using a small published
    ratings matrix with known ICC values (e.g. the Shrout & Fleiss classic 6-subject × 4-rater
    example, or another textbook ICC dataset). Feed the rater columns through the matrix-input icc
    expression, select the ICCType variant matching the published figure, and assert the estimate
    matches the published value within a loose tolerance; also assert the confidence-interval fields
    are present and bracket the estimate. Do not duplicate the smoke test — extend it. If pingouin
    or a reference lib is not available, rely on the published analytic constants (no new dependency).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/test_correlation.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - test_correlation.py feeds a known ratings matrix through icc and asserts the estimate vs a published ICC value
    - The selected ICCType variant is explicit in the test
    - CI fields are asserted present and bracket the estimate
  </acceptance_criteria>
  <done>The matrix-input icc expression is value-validated against a published ICC example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-dep import | scipy/statsmodels used only inside importorskip-guarded value tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | scipy/statsmodels (test-only, already in dev extras) | low | accept | Widely-audited scientific libs, test-only, never in the runtime wheel |
</threat_model>

<verification>
- Both edited test files parse as valid Python.
- New statistics symbols each carry a value assertion (reference-lib or analytic).
- Full maturin+pytest run deferred to 06-05 (orchestrator-run phase gate).
</verification>

<success_criteria>
Every newly exposed statistics symbol (one_way via 06-01; two_way, RM-ANOVA, energy_distance_nd,
icc here) has a value-correctness assertion.
</success_criteria>

<output>
Create `.planning/phases/06-testing-validation/06-02-SUMMARY.md` when done.
</output>
