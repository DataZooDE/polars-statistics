---
phase: 06-testing-validation
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - tests/rust_api.rs
autonomous: true
requirements: [TEST-04]
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Each new statistics *_fit wrapper (one_way_anova_fit, two_way_anova_fit, repeated_measures_anova_fit, energy_distance_nd_fit) is exercised in rust_api.rs and asserts its output-struct schema"
    - "The 5 gamma_*_fit diagnostic wrappers are exercised and assert their output-struct schemas"
    - "cargo test --no-default-features --test rust_api passes"
  artifacts:
    - tests/rust_api.rs
  key_links:
    - "rust_api.rs imports the new *_fit symbols from polars_statistics::expressions"
---

<objective>
Add Rust-side integration tests to tests/rust_api.rs covering the newly exposed expression `*_fit`
wrappers and their output-type schemas — the statistics ANOVA/energy family and the 5 gamma_*
GLM diagnostics — which currently have zero rust_api coverage.

Purpose: Satisfy TEST-04 (Rust-side tests cover the new expression wrappers and output-type
schemas). This plan is executor-verifiable: `cargo test --no-default-features --test rust_api` runs
inside a subagent without stalling (unlike maturin/pytest).
Output: New #[test] functions in rust_api.rs invoking each new *_fit with valid inputs and
asserting the returned Struct series has the expected named fields.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-validation/06-CONTEXT.md
@.planning/phases/02-public-api-audit/02-API-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust-side tests for new statistics *_fit wrappers (ANOVA family + energy_distance_nd)</name>
  <files>tests/rust_api.rs</files>
  <read_first>
    - tests/rust_api.rs (existing helper fns: series_f64, scalar_*, assert_n_obs_nonzero, assert_f64_finite_or_zero; existing test structure and input-slice conventions)
    - src/expressions/parametric.rs (one_way_anova_fit, two_way_anova_fit, repeated_measures_anova_fit — exact input Series order and output struct field names)
    - src/expressions/modern.rs (energy_distance_nd_fit — input order + output fields; find via grep if in a different module)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (ANOVA / energy output struct field lists)
  </read_first>
  <action>
    Add a new #[test] fn (e.g. anova_and_energy_fits) that invokes one_way_anova_fit,
    two_way_anova_fit, repeated_measures_anova_fit, and energy_distance_nd_fit using the existing
    helper constructors for the input Series slice (matching each wrapper's documented input order —
    group Series plus any kind/parameter scalar as the last args, per the kruskal_wallis-style
    contract noted in STATE decisions). For each, assert the call returns Ok and that the resulting
    Struct series contains the expected named fields (assert the field is present and finite/zero
    via the existing assert_f64_finite_or_zero / assert_n_obs_nonzero helpers). Do not duplicate the
    Phase-3 smoke assertions if they already exist in rust_api.rs — grep first; extend if present.
  </action>
  <verify>
    <automated>cargo test --no-default-features --test rust_api anova_and_energy 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - rust_api.rs invokes one_way_anova_fit, two_way_anova_fit, repeated_measures_anova_fit, energy_distance_nd_fit
    - Each asserts its output Struct has the expected named fields
    - grep -c 'one_way_anova_fit' tests/rust_api.rs returns >= 1
    - cargo test --no-default-features --test rust_api passes
  </acceptance_criteria>
  <done>The four new statistics expression wrappers are exercised in rust_api.rs with schema
  assertions and the rust_api suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rust-side tests for the 5 gamma_*_fit GLM diagnostic wrappers</name>
  <files>tests/rust_api.rs</files>
  <read_first>
    - tests/rust_api.rs (existing GLM test block, e.g. glm_fits, and its input conventions)
    - src/expressions/regression.rs (gamma_dispersion_deviance_fit, gamma_dispersion_pearson_fit, gamma_pearson_chi_squared_fit, gamma_standardized_deviance_residuals_fit, gamma_standardized_pearson_residuals_fit — input order + output struct field names)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (REGR-05 diagnostic output schemas)
  </read_first>
  <action>
    Add a new #[test] fn (e.g. gamma_diagnostic_fits) that constructs a small positive-valued
    response and design matrix (Gamma requires positive y) and invokes each of the five gamma_*_fit
    wrappers with the correct input Series slice order. Assert each returns Ok and that the output
    Struct series exposes the expected named fields (dispersion scalar for the dispersion fns; a
    chi-squared scalar for pearson_chi_squared; per-observation residual arrays for the standardized
    residual fns). Use the existing finite/zero assertion helpers. Extend rather than duplicate any
    existing gamma coverage.
  </action>
  <verify>
    <automated>cargo test --no-default-features --test rust_api gamma_diagnostic 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - rust_api.rs invokes all 5 gamma_*_fit wrappers
    - Each asserts its output Struct field schema
    - grep -c 'gamma_standardized_deviance_residuals_fit' tests/rust_api.rs returns >= 1
    - cargo test --no-default-features --test rust_api passes
  </acceptance_criteria>
  <done>All five gamma_* diagnostic wrappers are exercised in rust_api.rs with schema assertions and
  the rust_api suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Pure Rust test additions; no new deps, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-RS | Tampering | tests/rust_api.rs (no new dependencies) | low | accept | Rust test-only additions using existing dev-dependencies; no supply-chain surface change |
</threat_model>

<verification>
- cargo test --no-default-features --test rust_api passes (executor-runnable in-subagent).
- New *_fit wrappers each have a schema assertion.
</verification>

<success_criteria>
Rust-side tests cover every new statistics ANOVA/energy wrapper and the 5 gamma_* diagnostic
wrappers, and the rust_api suite is green.
</success_criteria>

<output>
Create `.planning/phases/06-testing-validation/06-04-SUMMARY.md` when done.
</output>
