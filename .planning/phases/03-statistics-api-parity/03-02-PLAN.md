---
phase: 03-statistics-api-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/expressions/modern.rs
  - python/polars_statistics/exprs/modern.py
autonomous: true
requirements: [STAT-04]
estimate:
  tokens: 45000
  raw_tokens: 26000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "User can call ps.energy_distance_nd(x_cols, y_cols, n_permutations=, seed=) on multi-column (nD) samples and receive a statistic/p_value struct (STAT-04)"
    - "The existing 1D ps.energy_distance expression is unchanged"
  artifacts:
    - "src/expressions/modern.rs contains pub fn energy_distance_nd_fit + pl_energy_distance_nd shim"
    - "python/polars_statistics/exprs/modern.py contains energy_distance_nd builder"
  key_links:
    - "energy_distance_nd_fit reuses stats_output_dtype (statistic + p_value) — no new output_dtype, no output_types.rs edit"
    - "Python builder passes d then n_permutations then nullable seed then d X-columns then d Y-columns; Rust transposes column-per-dimension into row-per-observation Vec<Vec<f64>>"
---

<objective>
Expose the multi-dimensional `energy_distance_test` overload as a NEW Polars expression
`ps.energy_distance_nd`, keeping the existing 1D `energy_distance` untouched (STAT-04,
per D: STAT-04 exposes the nD overload as a new expression, existing 1D stays).

Purpose: deliver genuine multivariate distribution comparison — the real STAT-04 gap.
Output: `energy_distance_nd_fit` + `pl_energy_distance_nd` (modern.rs) and the
`energy_distance_nd` Python builder (modern.py). Registration + smoke test are handled in 03-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-statistics-api-parity/03-RESEARCH.md
@.planning/phases/03-statistics-api-parity/03-PATTERNS.md
@.planning/phases/03-statistics-api-parity/03-CONTEXT.md

# Verified this session: anofox_statistics::energy_distance_test is re-exported at the
# crate root (modern::{energy_distance_test, energy_distance_test_1d, ...}). Use the
# crate-root path `use anofox_statistics::energy_distance_test;`.
# Signature (RESEARCH energy.rs:157-178):
#   energy_distance_test(x: &[Vec<f64>], y: &[Vec<f64>], n_permutations: usize, seed: Option<u64>)
#   -> Result<EnergyDistanceResult { statistic, p_value, n_permutations }>
# Each observation is a Vec<f64> of length d; x and y share the same d.
</context>

<artifacts_produced>
New symbols this plan creates:
- Rust fit fn: `energy_distance_nd_fit` (pub fn, modern.rs)
- Rust FFI shim: `pl_energy_distance_nd` (private fn, output_type_func=stats_output_dtype)
- Python builder: `energy_distance_nd(x_cols: list, y_cols: list, n_permutations=999, seed=None)` (exprs/modern.py)
- Input contract (inputs order): [0]=d UInt32, [1]=n_permutations UInt32, [2]=seed UInt64 nullable, [3..3+d]=X feature Series, [3+d..3+2d]=Y feature Series
NOTE: registration in exprs/__init__.py and top-level __init__.py is OWNED BY 03-04 (avoids
__init__.py write-conflict with 03-01). This plan does NOT touch __init__.py.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: energy_distance_nd Rust wrapper + Python builder (STAT-04)</name>
  <files>src/expressions/modern.rs, python/polars_statistics/exprs/modern.py</files>
  <read_first>
    - .planning/phases/03-statistics-api-parity/03-RESEARCH.md (energy_distance_test nD signature + EnergyDistanceResult fields energy.rs:8-15; nD input encoding d/n_perm/seed/X-dims/Y-dims; multi-column null-handling pitfall 1; energy_distance_nd reuses stats_output_dtype)
    - .planning/phases/03-statistics-api-parity/03-PATTERNS.md (modern.rs energy_distance_fit analog lines 190-211; partial_cor_fit variable-arity loop; transpose column-per-dim to row-per-obs; modern.py energy_distance builder + nullable-seed pattern lines 423-453)
    - src/expressions/modern.rs (energy_distance_fit / pl_energy_distance / mmd_test_fit — exact templates; imports line 6)
    - src/expressions/correlation.rs (partial_cor_fit lines 167-211 — variable-arity inputs.get(3+i) loop)
    - python/polars_statistics/exprs/modern.py (energy_distance builder, _to_expr helper, seed_expr nullable pattern)
  </read_first>
  <behavior>
    - 2D samples X = two columns near 0, Y = two columns near 3, n_permutations=99, seed=42: statistic greater than 0, p_value in [0,1] (typically small for well-separated samples).
    - Identical X and Y: statistic small, p_value large — 03-04 asserts this; this task asserts callability + finite output only.
  </behavior>
  <action>
    In modern.rs add `use anofox_statistics::energy_distance_test;` (crate root). Add `pub fn energy_distance_nd_fit(inputs: &[Series]) -> PolarsResult<Series>`: read inputs[0].u32() to d (usize), inputs[1].u32() to n_permutations (usize, default 999), inputs[2].u64().get(0) to seed Option<u64>. Following partial_cor_fit's inputs.get(3+i) loop, collect d X-columns from inputs[3..3+d] and d Y-columns from inputs[3+d..3+2*d], each column via `.f64()?.into_no_null_iter().collect::<Vec<f64>>()`. Transpose column-major to row-major: n_x = length of X column 0; build `x: Vec<Vec<f64>>` where x[obs] = the d feature values at that obs index; same for y. Call `energy_distance_test(&x, &y, n_permutations, seed)`. On Ok use `generic_stats_output(result.statistic, result.p_value, "energy_distance_nd")`; on Err use `generic_stats_output(f64::NAN, f64::NAN, "energy_distance_nd")` (mirrors energy_distance_fit exactly).
    Add shim `#[polars_expr(output_type_func=stats_output_dtype)] fn pl_energy_distance_nd(inputs: &[Series]) -> PolarsResult<Series> { energy_distance_nd_fit(inputs) }`.
    In exprs/modern.py add `energy_distance_nd(x_cols, y_cols, n_permutations=999, seed=None) -> pl.Expr`: raise ValueError if len(x_cols) != len(y_cols); d=len(x_cols); build x_exprs/y_exprs via the existing _to_expr helper cast to Float64; per pitfall 1 apply one shared finite mask across all x columns (pl.all_horizontal) and a separate shared mask across all y columns so rows stay aligned within each sample; seed_expr = pl.lit(seed, dtype=pl.UInt64) if seed is not None else pl.lit(None, dtype=pl.UInt64); args = [pl.lit(d, dtype=pl.UInt32), pl.lit(n_permutations, dtype=pl.UInt32), seed_expr, then the d cleaned x exprs, then the d cleaned y exprs]; function_name="pl_energy_distance_nd"; returns_scalar=True. Docstring with Parameters/Returns/Examples per the energy_distance docstring, noting x_cols and y_cols must have equal length (dimension d).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && cargo build --features python 2>&1 | tail -5 && maturin develop 2>&1 | tail -3 && python -m pytest tests/ -k energy -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pub fn energy_distance_nd_fit" src/expressions/modern.rs` succeeds
    - `grep -q "pl_energy_distance_nd" src/expressions/modern.rs` succeeds
    - `grep -q "def energy_distance_nd" python/polars_statistics/exprs/modern.py` succeeds
    - `grep -q "pub fn energy_distance_fit" src/expressions/modern.rs` still succeeds (1D expression untouched)
    - `cargo build --features python` exits 0
    - `python -c "import polars as pl; from polars_statistics.exprs.modern import energy_distance_nd; d=pl.DataFrame({'x1':[0.,0.1,-0.1,0.2],'x2':[0.,-0.1,0.1,0.05],'y1':[3.,3.1,2.9,3.2],'y2':[3.,2.9,3.1,3.05]}); r=d.select(energy_distance_nd(['x1','x2'],['y1','y2'],n_permutations=99,seed=42)); v=r[0,0]; assert v['statistic']>0 and 0<=v['p_value']<=1"` exits 0
  </acceptance_criteria>
  <done>ps.energy_distance_nd (via the exprs.modern builder) is callable on 2D samples, returns a finite statistic and a p_value in [0,1], and the existing 1D energy_distance_fit is unchanged. Full registration + the TestEnergyDistanceNd smoke class land in 03-04.</done>
</task>

<task type="auto">
  <name>Task 2: clippy + fmt + ruff gate for energy_distance_nd additions</name>
  <files>src/expressions/modern.rs, python/polars_statistics/exprs/modern.py</files>
  <read_first>
    - .claude/CLAUDE.md (CI gates: clippy -D warnings, cargo fmt, ruff line 100 py39)
  </read_first>
  <action>
    Run cargo fmt and cargo clippy over the new nD energy code and fix any warnings (unused imports, needless collects, `.get(0)` to `.first()` where clippy suggests). Run ruff over modern.py to keep line-length 100 / py39 green. Formatting/lint only — no behavior change.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && cargo fmt --check && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -5 && ruff check python/polars_statistics/exprs/modern.py</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets --features python -- -D warnings` exits 0
    - `ruff check python/polars_statistics/exprs/modern.py` exits 0
  </acceptance_criteria>
  <done>energy_distance_nd Rust and Python additions pass clippy -D warnings, cargo fmt --check, and ruff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → Polars expression | User-provided f64 feature columns already inside the DataFrame; no I/O, no auth, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | energy_distance_nd_fit transpose on ragged/empty feature columns | low | mitigate | into_no_null_iter yields empty iter (no panic); crate returns Err on empty/mismatched samples; wrapper returns NaN stats struct on Err |
| T-03-05 | DoS | permutation test O(n_x * n_y * n_permutations) on large inputs | low | accept | documented as expensive in the builder docstring; no mitigation needed at the expression layer (user controls n_permutations) |
| T-03-06 | Tampering | x_cols/y_cols dimension mismatch | low | mitigate | Python builder raises ValueError when len(x_cols) != len(y_cols) before any FFI call |
</threat_model>

<verification>
- cargo build --features python succeeds with pl_energy_distance_nd compiled.
- maturin develop rebuilds so the exprs.modern.energy_distance_nd builder is callable.
- The nD callability check returns statistic > 0 and p_value in [0,1] on separated 2D samples.
- The existing 1D energy_distance_fit remains present and unchanged.
- cargo clippy -D warnings, cargo fmt --check, ruff green.
</verification>

<success_criteria>
- STAT-04: ps.energy_distance_nd computes the multivariate energy distance test via the Polars expression API.
- Existing 1D energy_distance expression untouched.
- clippy -D warnings, fmt --check, ruff all green.
</success_criteria>

<output>
Create `.planning/phases/03-statistics-api-parity/03-02-SUMMARY.md` when done.
</output>
