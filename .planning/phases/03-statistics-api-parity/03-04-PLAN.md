---
phase: 03-statistics-api-parity
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - python/polars_statistics/exprs/__init__.py
  - python/polars_statistics/__init__.py
  - tests/test_statistics_parity.py
  - tests/rust_api.rs
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04, STAT-05]
estimate:
  tokens: 55000
  raw_tokens: 32000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Every new expression (one_way_anova, two_way_anova, repeated_measures_anova, energy_distance_nd, icc) is importable from top-level polars_statistics and callable (STAT-01..05)"
    - "tests/test_statistics_parity.py has a smoke class per capability that passes (shape + known-value sanity)"
    - "The full pytest suite and Rust test suite pass with no regressions"
  artifacts:
    - "python/polars_statistics/exprs/__init__.py and python/polars_statistics/__init__.py export energy_distance_nd (and confirm the ANOVA + icc names resolve)"
    - "tests/test_statistics_parity.py contains TestOneWayAnova, TestTwoWayAnova, TestRmAnova, TestEnergyDistanceNd, TestIcc"
    - "tests/rust_api.rs contains a smoke test per new *_fit fn"
  key_links:
    - "energy_distance_nd registration in both __init__ files makes ps.energy_distance_nd resolve (03-02 deferred this to avoid a Wave-1 __init__ write conflict with 03-01)"
    - "maturin develop rebuild precedes the pytest smoke suite so Python sees all five new expressions"
---

<objective>
Finish registration for `energy_distance_nd`, confirm all five new statistics expressions
resolve from top-level `polars_statistics`, add the full smoke-test suite (one class per
capability, shape + known-value sanity) plus Rust-side smoke tests for each new `*_fit`, then
run the full suites and CI gates green (STAT-01..05 integration).

Purpose: prove end-to-end parity — every new capability is callable and value-sane — and lock
the phase gate (clippy -D warnings, fmt, ruff, full pytest + Rust suites).
Output: complete registration, tests/test_statistics_parity.py (5 classes), Rust smoke tests,
all suites green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-statistics-api-parity/03-RESEARCH.md
@.planning/phases/03-statistics-api-parity/03-PATTERNS.md

# Depends on 03-01 (ANOVA fns + ANOVA registration + ANOVA smoke classes already added),
# 03-02 (energy_distance_nd_fit + builder — NOT yet registered in __init__), and
# 03-03 (real icc_fit + rewritten icc builder — icc already registered).
# This plan OWNS the energy_distance_nd __init__ registration and the remaining smoke classes
# (TestEnergyDistanceNd, TestIcc) plus the Rust-side smoke tests. It runs in Wave 3 so its
# __init__.py edits never race the ANOVA __init__ edits from 03-01 (Wave 1).
</context>

<artifacts_produced>
This plan produces:
- energy_distance_nd added to the modern import block + __all__ in exprs/__init__.py and top-level __init__.py
- tests/test_statistics_parity.py completed with TestEnergyDistanceNd and TestIcc (TestOneWayAnova/TwoWay/RmAnova come from 03-01)
- tests/rust_api.rs: one #[test] per new fit fn (one_way_anova_fit, two_way_anova_fit, repeated_measures_anova_fit, energy_distance_nd_fit, icc_fit) asserting non-NaN plausible output on hardcoded input
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Register energy_distance_nd and verify all five names resolve</name>
  <files>python/polars_statistics/exprs/__init__.py, python/polars_statistics/__init__.py</files>
  <read_first>
    - .planning/phases/03-statistics-api-parity/03-PATTERNS.md (exprs/__init__.py import-block + __all__ patterns lines 500-537; modern import block)
    - python/polars_statistics/exprs/__init__.py (modern import block + __all__ — after 03-01 added the ANOVA names)
    - python/polars_statistics/__init__.py (exprs import block + __all__)
  </read_first>
  <action>
    Add energy_distance_nd to the modern import block AND to __all__ in exprs/__init__.py, and to the from polars_statistics.exprs import block AND __all__ in top-level __init__.py, mirroring how energy_distance is listed. Do not disturb the ANOVA entries added by 03-01 or the existing icc entry. After editing, rebuild with maturin develop and confirm all five new names resolve as ps attributes.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && maturin develop 2>&1 | tail -3 && python -c "import polars_statistics as ps; [getattr(ps, n) for n in ['one_way_anova','two_way_anova','repeated_measures_anova','energy_distance_nd','icc']]; print('all resolve')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "energy_distance_nd" python/polars_statistics/exprs/__init__.py` succeeds
    - `grep -q "energy_distance_nd" python/polars_statistics/__init__.py` succeeds
    - `python -c "import polars_statistics as ps; [getattr(ps, n) for n in ['one_way_anova','two_way_anova','repeated_measures_anova','energy_distance_nd','icc']]"` exits 0
  </acceptance_criteria>
  <done>All five new statistics expressions (one_way_anova, two_way_anova, repeated_measures_anova, energy_distance_nd, icc) resolve as attributes of top-level polars_statistics.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Python smoke suite (5 capability classes)</name>
  <files>tests/test_statistics_parity.py</files>
  <read_first>
    - .planning/phases/03-statistics-api-parity/03-RESEARCH.md (Validation Architecture — Phase Requirements to Test Map; smoke-test design steps; known-input sanity examples for one_way_anova)
    - .planning/phases/03-statistics-api-parity/03-PATTERNS.md (tests/test_statistics_parity.py smoke class template lines 546-566)
    - tests/test_statistics_parity.py (TestOneWayAnova/TestTwoWayAnova/TestRmAnova added by 03-01)
    - tests/conftest.py (existing fixtures/import style)
  </read_first>
  <behavior>
    - TestEnergyDistanceNd: 2D X near 0, 2D Y near 3, seed fixed — result shape (1,1); struct has statistic and p_value; statistic > 0; p_value in [0,1]. Identical-distribution case: p_value not tiny (sanity, tolerant bound).
    - TestIcc: 4 subjects x 3 raters strong agreement — icc in [-1,1], not NaN, n_subjects 4, n_raters 3; icc_type="icc3" differs from "icc2".
    - Confirm (or add if missing) TestOneWayAnova/TestTwoWayAnova/TestRmAnova assertions cover shape + plausible F/p from 03-01.
  </behavior>
  <action>
    Add TestEnergyDistanceNd and TestIcc classes to tests/test_statistics_parity.py using hardcoded (non-random) known-input DataFrames per <behavior>, following the smoke-class template. Each test calls df.select(ps.<expr>(...)), asserts result.shape == (1,1), checks the expected struct field names are present, and asserts plausible numeric ranges (no NaN where a value is expected, p_value in [0,1], F/statistic >= 0). No R reference values (that is Phase 6). Ensure the file has all five capability classes.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && python -m pytest tests/test_statistics_parity.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class TestEnergyDistanceNd" tests/test_statistics_parity.py` succeeds
    - `grep -q "class TestIcc" tests/test_statistics_parity.py` succeeds
    - `grep -cE "class Test(OneWayAnova|TwoWayAnova|RmAnova|EnergyDistanceNd|Icc)" tests/test_statistics_parity.py` returns 5
    - `python -m pytest tests/test_statistics_parity.py -x` passes (all five classes)
  </acceptance_criteria>
  <done>tests/test_statistics_parity.py has all five capability smoke classes and the whole file passes — each new expression is proven callable with value-sane output.</done>
</task>

<task type="auto">
  <name>Task 3: Rust smoke tests + full-suite phase gate</name>
  <files>tests/rust_api.rs</files>
  <read_first>
    - .planning/phases/03-statistics-api-parity/03-RESEARCH.md (Sampling Rate — phase gate = full suite + clippy -D warnings + fmt --check)
    - tests/rust_api.rs (existing #[test] style — how Series inputs are constructed and fit fns are called directly)
    - src/expressions/parametric.rs, src/expressions/modern.rs, src/expressions/correlation.rs (the new pub fn *_fit signatures to call directly)
  </read_first>
  <action>
    Add one #[test] per new fit fn to tests/rust_api.rs, constructing the exact input Series each fit expects (per the input contracts in the 03-01/02/03 artifacts_produced) and asserting the returned struct's first row has non-NaN plausible values: one_way_anova_fit (3 group Series + "fisher" literal → statistic finite > 0), two_way_anova_fit (value + two u32 factor Series → a_ss finite), repeated_measures_anova_fit (balanced value/subject/condition u32 Series + bool → ws_f finite), energy_distance_nd_fit (d=2 + n_perm + seed + 2 X-dim + 2 Y-dim Series → statistic finite), icc_fit (n_raters=3 + "icc2" + 3 rater Series → icc finite in [-1,1]). Mirror how existing rust_api.rs tests extract struct fields. Then run the full Rust suite, the full pytest suite, and the CI gates.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && cargo test --features python 2>&1 | tail -10 && maturin develop 2>&1 | tail -3 && python -m pytest tests/ -q 2>&1 | tail -10 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -5 && cargo fmt --check && ruff check python/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "one_way_anova_fit|two_way_anova_fit|repeated_measures_anova_fit|energy_distance_nd_fit|icc_fit" tests/rust_api.rs` returns at least 5
    - `cargo test --features python` exits 0 (all Rust tests pass, no regressions)
    - `python -m pytest tests/` exits 0 (full pytest suite green, no regressions)
    - `cargo clippy --all-targets --features python -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
    - `ruff check python/` exits 0
  </acceptance_criteria>
  <done>Every new *_fit fn has a Rust smoke test; the full Rust + pytest suites pass with no regressions; clippy -D warnings, fmt --check, and ruff are green — the phase gate is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → Polars expression | Registration + tests only; user-provided numeric columns, no I/O, no auth, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Tampering | __init__ registration typo silently shadows/omits an expression | low | mitigate | Task 1 asserts all five names resolve via getattr before tests run; Task 3 full pytest suite fails if any expression is unreachable |
| T-03-11 | Repudiation | smoke tests pass on all-NaN output (false green) | low | mitigate | each smoke assertion checks non-NaN + plausible range, not merely shape, so a regressed all-NaN result (e.g. ICC re-stubbing) fails the test |
</threat_model>

<verification>
- All five new expressions resolve from top-level polars_statistics.
- tests/test_statistics_parity.py has 5 passing capability classes.
- tests/rust_api.rs has a passing smoke test per new fit fn.
- cargo test --features python and full pytest suite pass with no regressions.
- cargo clippy -D warnings, cargo fmt --check, ruff check python/ all green.
</verification>

<success_criteria>
- STAT-01..05: every new statistics function is callable via the Polars/Python API and value-sane.
- Full Rust + Python suites green; CI gates (clippy/fmt/ruff) green.
</success_criteria>

<output>
Create `.planning/phases/03-statistics-api-parity/03-04-SUMMARY.md` when done.
</output>