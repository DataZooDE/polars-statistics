---
phase: 03-statistics-api-parity
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/expressions/correlation.rs
  - src/expressions/output_types.rs
  - python/polars_statistics/exprs/correlation.py
autonomous: true
requirements: [STAT-05]
estimate:
  tokens: 60000
  raw_tokens: 35000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "User can call ps.icc(*rater_cols, icc_type=...) on a subjects x raters matrix and receive a real (non-NaN) ICC struct with icc/f_value/df1/df2/p_value/ci_lower/ci_upper/n_subjects/n_raters (STAT-05)"
    - "The all-NaN icc_fit stub in correlation.rs is fully replaced by the real matrix-input implementation"
  artifacts:
    - "src/expressions/correlation.rs contains the real icc_fit (matrix input) + parse_icc_type + icc_error_output"
    - "src/expressions/output_types.rs contains icc_output_dtype"
    - "python/polars_statistics/exprs/correlation.py contains the rewritten icc(*rater_cols, icc_type=) builder"
  key_links:
    - "pl_icc output_type_func changes from correlation_output_dtype to icc_output_dtype; struct field order in icc_fit matches icc_output_dtype"
    - "Python builder passes n_raters then icc_type string then one Series per rater; Rust collects rater-indexed columns and transposes to subject-indexed data before calling icc()"
---

<objective>
Replace the all-NaN `icc_fit` stub in `src/expressions/correlation.rs` with a real
matrix-input ICC using the crate's `icc()` / `ICCType` / `ICCResult` API, add `icc_output_dtype`,
and rewrite the Python `icc` builder to accept multiple rater columns + an `icc_type` parameter
(STAT-05, per D: implement real matrix-input ICC replacing the stub).

Purpose: the ICC binding currently returns all-NaN regardless of input — a NaN-returning binding
is effectively unexposed. This makes ICC a genuinely usable capability.
Output: real `icc_fit`, `parse_icc_type`, `icc_error_output`, `icc_output_dtype`, and the
rewritten `icc` Python builder. `icc` is already registered in __init__ (no registration change).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-statistics-api-parity/03-RESEARCH.md
@.planning/phases/03-statistics-api-parity/03-PATTERNS.md
@.planning/phases/03-statistics-api-parity/03-CONTEXT.md

# Verified this session: anofox_statistics::{icc, ICCType, ICCResult} are re-exported at the
# crate root (correlation::{icc, ICCType, ICCResult, ...}). Use `use anofox_statistics::{icc, ICCType, ICCResult};`.
# Signature (RESEARCH icc.rs:99-185):
#   icc(data: &[Vec<f64>], icc_type: ICCType) -> Result<ICCResult>   # rows=subjects, cols=raters
# ICCType variants: ICC1, ICC2 (default), ICC3, ICC1k, ICC2k, ICC3k.
# ICCResult: icc, icc_type, f_value, df1, df2, p_value, conf_int_lower, conf_int_upper,
#            n_subjects, n_raters, method (String).
# WAVE 2 REASON: this plan edits output_types.rs which 03-01 also edits in Wave 1 — serialized to avoid a write conflict.
</context>

<artifacts_produced>
Changed/new symbols this plan creates:
- Rewritten Rust fit fn: `icc_fit` (pub fn, correlation.rs) — CHANGED input contract:
  [0]=n_raters UInt32, [1]=icc_type String, [2..2+n_raters]=one f64 Series per rater (rows=subjects)
  (old contract values/icc_type/conf_level is REMOVED)
- Rust helpers: `parse_icc_type(&str) -> ICCType`, `icc_error_output()` (correlation.rs)
- Changed shim: `pl_icc` output_type_func changes correlation_output_dtype to icc_output_dtype
- Output schema: `icc_output_dtype` (output_types.rs) — icc, f_value, df1, df2, p_value, ci_lower, ci_upper (F64), n_subjects, n_raters (UInt32)
- Rewritten Python builder: `icc(*rater_cols, icc_type="icc2")` (exprs/correlation.py) — REPLACES old (values, icc_type, conf_level) signature
NOTE: `icc` is already imported/exported in exprs/__init__.py and top-level __init__.py — no registration change.
icc_type/method (String/enum) from ICCResult are intentionally EXCLUDED from the output struct (scalar-only struct fields).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace icc_fit stub with real matrix-input ICC (STAT-05)</name>
  <files>src/expressions/output_types.rs, src/expressions/correlation.rs, python/polars_statistics/exprs/correlation.py</files>
  <read_first>
    - .planning/phases/03-statistics-api-parity/03-RESEARCH.md (icc signature icc.rs:99-185; ICCType variants icc.rs:10-24; ICCResult fields icc.rs:41-64; icc_output_dtype schema; ICC input=multiple rater Series; transpose rater-indexed to subject-indexed; multi-column null pitfall 1; ICCType import path pitfall 6)
    - .planning/phases/03-statistics-api-parity/03-PATTERNS.md (correlation.rs partial_cor_fit variable-arity analog lines 249-286; current icc stub to DELETE lines 295-311; icc_error_output helper pattern lines 337-349; correlation_output_dtype/tost_output_dtype schema analog; correlation.py icc rewrite lines 463-493; matrix transpose snippet lines 324-332)
    - src/expressions/correlation.rs (imports lines 6-11; partial_cor_fit lines 167-211; correlation_error_output; current icc_fit stub + pl_icc)
    - src/expressions/output_types.rs (correlation_output_dtype + tost_output_dtype analogs)
    - python/polars_statistics/exprs/correlation.py (current icc def + partial_cor varargs builder)
  </read_first>
  <behavior>
    - 4 subjects x 3 raters with strong agreement: icc in [-1,1] and NOT NaN, f_value finite, ci_lower and ci_upper finite, n_subjects equals 4, n_raters equals 3.
    - icc_type "icc3" vs default "icc2" produce different icc values on the same data (parameter honored).
    - Empty / single-rater input: returns the all-NaN error struct with n_subjects/n_raters 0, no panic.
  </behavior>
  <action>
    Add `icc_output_dtype(_: &[Field]) -> PolarsResult<Field>` to output_types.rs mirroring correlation_output_dtype: fields in order icc(F64), f_value(F64), df1(F64), df2(F64), p_value(F64), ci_lower(F64), ci_upper(F64), n_subjects(UInt32), n_raters(UInt32); outer Field name "icc".
    In correlation.rs add `use anofox_statistics::{icc, ICCType, ICCResult};` (crate root) and import icc_output_dtype from output_types. Add `parse_icc_type(s: &str) -> ICCType` matching to_lowercase: "icc1"=>ICC1, "icc3"=>ICC3, "icc1k"=>ICC1k, "icc2k"=>ICC2k, "icc3k"=>ICC3k, other => ICC2 (default matches crate Default).
    Replace the entire stub body of `icc_fit`: read inputs[0].u32() to n_raters (usize), inputs[1].str() to icc_type_str then parse_icc_type. Following partial_cor_fit's inputs.get(2+i) loop, collect n_raters rater columns (each into_no_null_iter collect to Vec f64) into a rater-indexed matrix. Guard: if matrix empty or matrix[0] empty return icc_error_output(). Transpose to subject-indexed: n_subjects = matrix[0].len(); build data as Vec of Vec f64 where data[subject] holds the n_raters scores for that subject. Call `icc(&data, icc_type)`. On Ok build StructChunked "icc" with fields icc, f_value, df1, df2, p_value, conf_int_lower (as ci_lower), conf_int_upper (as ci_upper), n_subjects as u32, n_raters as u32 — do NOT include icc_type or method. On Err call icc_error_output().
    Add `fn icc_error_output()` returning the same struct shape all-NaN with n_subjects/n_raters 0u32 (mirror correlation_error_output structure).
    Change the shim to `#[polars_expr(output_type_func=icc_output_dtype)] fn pl_icc(...)`.
    In exprs/correlation.py REPLACE the current icc def with `icc(*rater_cols, icc_type="icc2")` (Literal of the six variants): convert each rater str to pl.col; per pitfall 1 build a shared mask = pl.all_horizontal of each column is_finite() and filter every column by that same mask (row alignment); args = [pl.lit(len(rater_cols), dtype=pl.UInt32), pl.lit(icc_type, dtype=pl.String), then the cleaned rater columns]; function_name="pl_icc"; returns_scalar=True. Update the docstring: subjects are rows, each argument is a rater column, list the six icc_type options and the returned struct fields.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && cargo build --features python 2>&1 | tail -5 && maturin develop 2>&1 | tail -3 && python -m pytest tests/test_correlation.py -k icc -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pub fn icc_fit" src/expressions/correlation.rs` succeeds
    - `grep -q "parse_icc_type" src/expressions/correlation.rs` succeeds
    - `grep -Lq "TODO: Implement proper ICC" src/expressions/correlation.rs` (stub TODO comment is gone)
    - `grep -q "icc_output_dtype" src/expressions/output_types.rs` succeeds
    - `grep -q "output_type_func=icc_output_dtype" src/expressions/correlation.rs` succeeds
    - `cargo build --features python` exits 0
    - Running the ICC callability check script (below) exits 0 and prints icc in [-1,1], n_subjects=4, n_raters=3
  </acceptance_criteria>
  <done>ps.icc returns a real non-NaN ICC struct on a subjects x raters matrix with correct n_subjects/n_raters, honors the icc_type parameter, and handles degenerate input without panicking. The all-NaN stub is gone.</done>
</task>

<task type="auto">
  <name>Task 2: clippy + fmt + ruff gate for ICC additions</name>
  <files>src/expressions/correlation.rs, src/expressions/output_types.rs, python/polars_statistics/exprs/correlation.py</files>
  <read_first>
    - .claude/CLAUDE.md (CI gates: clippy -D warnings, cargo fmt, ruff line 100 py39)
  </read_first>
  <action>
    Write a small ICC callability check script (subjects x raters DataFrame, call ps.icc, assert icc is finite and in [-1,1], n_subjects/n_raters correct) and run it against the freshly built extension. Then run cargo fmt and cargo clippy over the ICC code and fix any warnings (unused ICCResult import if not referenced by name — use the field access on the Ok binding instead, needless collects, `.get(0)` to `.first()`). Run ruff over correlation.py. Formatting/lint only after the callability check passes.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && cargo fmt --check && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -5 && ruff check python/polars_statistics/exprs/correlation.py</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets --features python -- -D warnings` exits 0
    - `ruff check python/polars_statistics/exprs/correlation.py` exits 0
  </acceptance_criteria>
  <done>ICC Rust and Python additions pass clippy -D warnings, cargo fmt --check, and ruff; the ICC callability check returns a real non-NaN struct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → Polars expression | User-provided f64 rater columns already inside the DataFrame; no I/O, no auth, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | icc_fit transpose on empty / single-rater / ragged matrix | low | mitigate | wrapper guards empty matrix and returns icc_error_output(); crate validates equal row lengths and finite values, returns Err handled by the NaN struct |
| T-03-08 | Tampering | multi-column rater null misalignment | low | mitigate | Python builder applies one shared all_horizontal finite mask across all rater columns so rows stay aligned (pitfall 1) |
| T-03-09 | Information | invalid icc_type string | low | mitigate | parse_icc_type falls back to ICC2 default (matches crate Default); no error surfaced to user |
</threat_model>

<verification>
- cargo build --features python succeeds with the real icc_fit compiled and pl_icc using icc_output_dtype.
- maturin develop rebuilds so ps.icc returns the new struct schema.
- The ICC callability check returns a finite icc in [-1,1] with n_subjects/n_raters correct.
- The stub TODO comment is gone from correlation.rs.
- cargo clippy -D warnings, cargo fmt --check, ruff green.
</verification>

<success_criteria>
- STAT-05 (ICC portion): ps.icc computes a real matrix-input ICC via the Polars expression API, replacing the all-NaN stub.
- icc_type parameter selects among the six ICC variants.
- clippy -D warnings, fmt --check, ruff all green.
</success_criteria>

<output>
Create `.planning/phases/03-statistics-api-parity/03-03-SUMMARY.md` when done.
</output>