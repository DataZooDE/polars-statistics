---
phase: 02-public-api-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-public-api-audit/02-API-AUDIT.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "02-API-AUDIT.md enumerates every public anofox-statistics 0.4.2 item with an exposed? verdict (AUDIT-01)"
    - "02-API-AUDIT.md enumerates every public anofox-regression 0.5.13 capability with an exposed? verdict (AUDIT-02)"
    - "Each gap row records its target exposure surface (expression / PyModel / both / util)"
    - "All 21 known candidates from CONTEXT.md appear with an explicit present-or-absent verdict"
    - "The 3 open scope questions from RESEARCH.md are recorded as deferred Phase 3/4 decisions, not dropped"
    - "The ICC stub is flagged as exposed-but-non-functional (distinct from a normal gap)"
    - "Internal-only pub items (LOWESS helper, rank, error types) are marked low-priority / not-a-user-gap"
  artifacts:
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md
  key_links:
    - "02-API-AUDIT.md → consumed by Phase 3 (statistics parity) and Phase 4 (regression parity) planners as authoritative scope"
    - "Every gap row → traceable to a pub declaration in the cited crate source file"
---

<objective>
Synthesize the completed enumeration in 02-RESEARCH.md into the authoritative,
per-crate gap list `.planning/phases/02-public-api-audit/02-API-AUDIT.md`. This
document is the single deliverable of Phase 2 and defines the exact parity scope
consumed by Phase 3 (statistics) and Phase 4 (regression).

Purpose: Give the two downstream parity phases an unambiguous, verdict-per-item
scope list — which public functions/types in each upgraded crate are already
exposed via the Polars/Python API and which are gaps, with the target exposure
surface recorded for each gap.

Output: `02-API-AUDIT.md` — per-crate markdown tables (one row per public fn/type:
name, kind, exposed?, target surface, priority, notes), grouped by crate then
category, plus a known-candidate checklist, a requirement→gap cross-reference, and
recorded deferred scope questions. No code or wrapper changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-api-audit/02-CONTEXT.md
@.planning/phases/02-public-api-audit/02-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write 02-API-AUDIT.md from the RESEARCH.md enumeration tables</name>
  <files>.planning/phases/02-public-api-audit/02-API-AUDIT.md</files>

  <read_first>
    - .planning/phases/02-public-api-audit/02-RESEARCH.md — the enumeration source; the `## Complete Enumeration Tables`, `## Summary Counts`, `## Validation Architecture` (Known-Candidate Checklist), and `## Open Questions` sections ARE the primary input. Copy the per-category tables from here.
    - .planning/phases/02-public-api-audit/02-CONTEXT.md — locked decisions on artifact location/format, "exposed" definition (reachable via EITHER a Polars expression OR a PyModel class), output granularity (one row per public fn/type), and the discretion rules for internal-only items.
    - src/expressions/correlation.rs — re-read the `icc_fit` body to confirm assumption A1 (returns all-NaN with a `TODO` matrix-input comment) before flagging ICC as stub-only. If the TODO was resolved since research, correct the ICC verdict accordingly.
  </read_first>

  <action>
    Write `.planning/phases/02-public-api-audit/02-API-AUDIT.md` as a planning
    artifact synthesizing 02-RESEARCH.md's enumeration into the final gap list.
    Follow the CONTEXT.md-locked format: per-crate markdown tables, one row per
    public fn/type, columns name / kind / exposed? (yes/no) / target surface
    (expression | PyModel | both | util) / priority / notes, grouped by crate then
    category.

    Structure the document with these sections in order:

    1. `## Overview` — confirm the two audited crate versions (anofox-statistics
       0.4.2, anofox-regression 0.5.13), state the enumeration method (authoritative
       scan of `lib.rs` / module-level `pub use` re-exports in the Cargo registry
       cache, cross-referenced against `src/expressions/*.rs`, `src/pymodels/*.rs`,
       and `python/polars_statistics/__init__.py` as the ground-truth "exposed?"
       reference), and reproduce both Summary Counts tables from RESEARCH.md.

    2. `## anofox-statistics 0.4.2 — Gap List` (satisfies AUDIT-01) — reproduce the
       per-category tables from RESEARCH.md (Parametric, Nonparametric,
       Distributional, Correlation, Modern, Categorical, Equivalence/TOST, Forecast,
       Resampling, Error types). Preserve every row and its exposed?/surface/priority
       verdict. Add a `### Internal-only items (not Phase 3 action items)` subsection
       collecting the LOW-priority util rows (`rank`, `distance_cor` standalone,
       `PermutationEngine`, `PermutationResult`, `Result`, `StatError`), each marked
       not-a-user-gap.

    3. `## anofox-regression 0.5.13 — Gap List` (satisfies AUDIT-02) — reproduce the
       Core Types, Inference/HC, Solvers-Already-Exposed, Solvers-NOT-Exposed (Gaps),
       and Diagnostics tables. Add a `### Internal-only items` subsection for the LOW
       util rows (GLM family/link configs, NA-handling types, matrix-inverse helpers,
       `CoefficientInference`, `AidClassifierBuilder`, `RegressionError`, etc.).

    4. `## Known-Candidate Checklist` — reproduce the 21-row checklist from
       RESEARCH.md `## Validation Architecture` verbatim (Candidate / Present in
       Crate? / Exposed? / Resolved). Every one of the 21 candidates must carry an
       explicit verdict. Note LOWESS as internal-only (not in `solvers/mod.rs` pub
       use — not a user-facing gap) and HC/energy_distance as PARTIAL.

    5. `## Special-Case Flags` — call out two items distinctly from normal gaps:
       (a) the ICC stub: `icc_fit` is bound and `icc` appears in `__init__.py` but
       returns all-NaN with a TODO — label it "exposed-but-stubbed → needs real
       matrix-input implementation in Phase 3", not a missing-binding gap; and
       (b) `FactorSummary` — pub output field of `FittedGlmm::factors()`, so it is
       HIGH-priority user-facing (part of GLMM output), NOT internal despite its name.

    6. `## Deferred Scope Questions (for Phase 3/4 planning)` — record the three open
       questions from RESEARCH.md `## Open Questions` as explicit deferred decisions,
       NOT dropped: (1) STAT-04 — does it require the nD `energy_distance_test`
       overload or is the existing 1D `energy_distance_test_1d` wrapper sufficient?;
       (2) is `PassiveAggressiveRegressor` / `MomentAccumulator` PyModel-only (stateful
       streaming does not map to per-group Polars expressions)?; (3) is the ICC stub
       fix in-scope for Phase 3 as an "unexposed capability"? For each, state the
       recommendation from RESEARCH.md and mark it "deferred to Phase 3/4 planning".

    7. `## Cross-Reference: Requirement → Gap` — map STAT-01→one_way_anova,
       STAT-02→two_way_anova, STAT-03→repeated_measures_anova,
       STAT-04→energy_distance_test (nD) + 1D-sufficiency question,
       STAT-05→ICC stub + any remaining statistics gaps,
       REGR-01→GlmmRegressor, REGR-02→PSplineRegressor, REGR-03→GammaRegressor,
       REGR-04→HcInference + compute_hc_inference, REGR-05→GLM dispersion/residual
       variants (most diagnostics already exposed),
       REGR-06→TheilSen/RANSAC/BayesianRidge/ARD/LARS/PassiveAggressive/MomentAccumulator.

    Apply the CONTEXT.md discretion rules: keep borderline public-but-internal items
    in the tables but mark them LOW priority / util. Do NOT introduce any new gaps not
    grounded in a RESEARCH.md row, and do NOT drop any RESEARCH.md gap row. This is a
    documentation task only — make no changes to any `src/` or `python/` file.
  </action>

  <verify>
    <automated>test -f .planning/phases/02-public-api-audit/02-API-AUDIT.md &amp;&amp; grep -q "anofox-statistics 0.4.2" .planning/phases/02-public-api-audit/02-API-AUDIT.md &amp;&amp; grep -q "anofox-regression 0.5.13" .planning/phases/02-public-api-audit/02-API-AUDIT.md &amp;&amp; grep -q "Known-Candidate Checklist" .planning/phases/02-public-api-audit/02-API-AUDIT.md &amp;&amp; grep -q "Deferred Scope Questions" .planning/phases/02-public-api-audit/02-API-AUDIT.md &amp;&amp; grep -qi "one_way_anova" .planning/phases/02-public-api-audit/02-API-AUDIT.md &amp;&amp; grep -qi "GlmmRegressor" .planning/phases/02-public-api-audit/02-API-AUDIT.md &amp;&amp; grep -qi "HcInference" .planning/phases/02-public-api-audit/02-API-AUDIT.md</automated>
    <human-check>Skim 02-API-AUDIT.md: every gap row cites name/kind/exposed?/surface; all 21 known candidates carry a verdict; ICC stub and FactorSummary are flagged as special cases; the 3 deferred questions are recorded; no `src/` or `python/` file was modified (git status shows only the audit doc).</human-check>
  </verify>

  <acceptance_criteria>
    - 02-API-AUDIT.md exists at .planning/phases/02-public-api-audit/02-API-AUDIT.md.
    - It contains a per-crate table for anofox-statistics 0.4.2 (AUDIT-01) and one for anofox-regression 0.5.13 (AUDIT-02), each row carrying columns name / kind / exposed? / target surface.
    - All 21 known candidates from the RESEARCH.md checklist appear with an explicit present-or-absent (and exposed?) verdict.
    - The 3 deferred scope questions (nD energy_distance_test; PassiveAggressive/MomentAccumulator PyModel-only; ICC stub in-scope for Phase 3) are recorded under a "Deferred Scope Questions" heading, not dropped.
    - The ICC stub is flagged "exposed-but-stubbed" and the LOWESS helper is flagged internal-only / not-a-user-gap.
    - A "Cross-Reference: Requirement → Gap" section maps every STAT-* and REGR-* requirement to its gap(s).
    - `git status` shows no changes under `src/` or `python/` — the only new/changed file is the audit doc.
  </acceptance_criteria>

  <done>
    02-API-AUDIT.md is written, covers both crates with per-item verdicts and target
    surfaces, resolves all 21 known candidates, records the 3 deferred scope questions,
    flags the ICC stub and FactorSummary special cases, and cross-references every
    STAT-*/REGR-* requirement — with no code changes. The automated verify passes and
    Phases 3/4 have an unambiguous scope list.
  </done>

  <reversibility rating="reversible">Documentation artifact; regenerable from RESEARCH.md if the format needs revision.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase writes a single markdown planning document. No runtime input path, no network, no new code, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | .planning/phases/02-public-api-audit/02-API-AUDIT.md content accuracy | low | accept | Doc-only artifact; accuracy safeguarded by verify grep + human skim against RESEARCH.md source citations. No security impact. |

No package installs occur in this phase (RESEARCH.md `## Package Legitimacy Audit`: not applicable), so no supply-chain (T-{phase}-SC) checkpoint is required.
</threat_model>

<verification>
- `02-API-AUDIT.md` exists and contains both per-crate gap tables (AUDIT-01, AUDIT-02).
- All 21 known candidates carry an explicit verdict.
- The 3 deferred scope questions are recorded; ICC stub + LOWESS special cases flagged.
- Requirement → gap cross-reference present for all STAT-*/REGR-*.
- `git status` confirms no `src/` or `python/` changes.
</verification>

<success_criteria>
- Phase 2 Success Criterion 1: statistics gap list enumerated (AUDIT-01) ✓
- Phase 2 Success Criterion 2: regression gap list enumerated (AUDIT-02) ✓
- Phase 2 Success Criterion 3: each gap records its target exposure surface ✓
- Phase 2 Success Criterion 4: all known candidates confirmed present-or-absent ✓
</success_criteria>

<output>
Create `.planning/phases/02-public-api-audit/02-01-SUMMARY.md` when done.

## Artifacts this phase produces
- `.planning/phases/02-public-api-audit/02-API-AUDIT.md` — the authoritative gap list consumed by Phase 3 and Phase 4 planners.
</output>
