---
phase: 04-regression-api-parity
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pymodels/py_glmm.rs
  - src/pymodels/py_pspline.rs
  - src/pymodels/mod.rs
  - src/lib.rs
  - python/polars_statistics/__init__.py
  - tests/test_glmm.py
  - tests/test_pspline.py
autonomous: true
requirements: [REGR-01, REGR-02]
estimate:
  tokens: 78000
  raw_tokens: 42000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "User can `from polars_statistics import GLMM`, call `GLMM.gaussian().fit(X, y, group)`, and get finite `fixed_effects` (REGR-01)"
    - "User can call `GLMM.gaussian().fit_crossed(X, y, [g1, g2])` and read per-factor summaries via `factors()` as a list of dicts (REGR-01, FactorSummary)"
    - "User can `from polars_statistics import PSpline`, call `PSpline().fit(X, y)` on a single-column X, and read `edf`, `sigma2`, and a smoothed `predict()` (REGR-02)"
  artifacts:
    - src/pymodels/py_glmm.rs
    - src/pymodels/py_pspline.rs
    - tests/test_glmm.py
    - tests/test_pspline.py
  key_links:
    - "PyGLMM and PyPSpline registered in mod.rs + lib.rs #[pymodule] + __init__.py — else not importable"
    - "GLMM.fit signature is (x, y, group) not (x, y); group arrives as Vec<u64> and is mapped to Vec<usize> before GlmmRegressor::fit"
    - "PSpline.fit rejects X with ncols != 1 (raises ValueError) before to_faer()"
---

<objective>
Add the two HIGH-priority novel models: `GLMM` (REGR-01, generalized linear mixed model with
single-factor `fit` and multi-factor `fit_crossed`, plus `FactorSummary` output) and `PSpline`
(REGR-02, penalized B-spline smoother). Both are PyModel classes following the established pattern;
they touch different new files and share only the three registration files (edited once, in Task 3).

Purpose: Deliver the two hardest new regressors while the tracer pattern (Plan 01) is fresh.
Output: `py_glmm.rs`, `py_pspline.rs`, registration edits, `tests/test_glmm.py`, `tests/test_pspline.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-regression-api-parity/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PyGLMM (fit / fit_crossed / factors / getters)</name>
  <files>src/pymodels/py_glmm.rs, tests/test_glmm.py</files>
  <read_first>
    - .planning/phases/04-regression-api-parity/04-RESEARCH.md (section "REGR-01: GlmmRegressor + FittedGlmm + FactorSummary" — factory methods `gaussian()`/`poisson()`/`binomial()`, builder setters `with_intercept`/`random_intercept`/`random_slopes`/`reml`/`max_iterations`/`tolerance`, `fit(x,y,group)` and `fit_crossed(x,y,groups)`, all `FittedGlmm` getters, and `FactorSummary` fields `n_levels`/`sd`/`blups`; AND "Pitfall 1" on the u64→usize group conversion)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "src/pymodels/py_glmm.rs" — struct, factory staticmethod, `fit` with group array, `fit_crossed`, and `factors()` list-of-dicts code excerpts)
    - src/pymodels/py_tweedie.rs (factory-staticmethod construction analog and getter guards)
    - src/pymodels/py_poisson.rs (GLM fit/predict analog)
  </read_first>
  <behavior>
    - Test: `GLMM.gaussian().fit(X, y, group.tolist())` returns `is_fitted()==True` and finite `fixed_effects`.
    - Test: `GLMM.gaussian().fit_crossed(X, y, [g1, g2])` yields `factors()` a list with >=2 dicts, each having keys `n_levels`, `sd`, `blups`.
    - Test: reading `fixed_effects` before fit raises.
  </behavior>
  <action>
    Create `PyGLMM` with `#[pyclass(name = "GLMM")]`. Fields: `family: String`, `with_intercept: bool`,
    `random_intercept: bool`, `random_slopes: Vec<usize>`, `reml: bool`, `max_iter: usize`, `tol: f64`,
    `fitted: Option<FittedGlmm>`. Provide three `#[staticmethod]` constructors `gaussian`, `poisson`,
    `binomial` (each `#[pyo3(signature = (with_intercept=true, reml=true, max_iter=100, tol=1e-8))]`)
    that set `family` and defaults per 04-PATTERNS. `fit` takes `(x: PyReadonlyArray2, y: PyReadonlyArray1,
    group: Vec<u64>)`; convert `group` to `Vec<usize>` via `.iter().map(|&g| g as usize).collect()`
    (per RESEARCH Pitfall 1 — accept u64 for 32/64-bit portability), dispatch the family via the
    string, build with the builder setters, then `model.fit(&x_mat, &y_col, &group_usize)` mapping
    errors to `PyValueError`. `fit_crossed` takes `groups: Vec<Vec<u64>>`, converts each to
    `Vec<usize>`, builds `Vec<&[usize]>` slices, and calls `model.fit_crossed(...)`. Expose getters
    reading `FittedGlmm`: `fixed_effects`, `std_errors`, `intercept`, `slopes`, `random_effects`,
    `random_sd`, `theta`, `sigma`, `deviance`, `log_likelihood`, `n_groups`, `converged`, `iterations`,
    plus a `predict_fixed(x)` method (marginal predictions). Expose `factors()` as
    `Vec<Bound<PyDict>>` — one dict per `FactorSummary` with `n_levels`, `sd`, and `blups`
    (as `PyArray1::from_slice`) — do NOT create a separate PyFactorSummary class (per RESEARCH
    "FactorSummary exposure strategy"). Write `tests/test_glmm.py` with the GLMM deviations shown in
    04-PATTERNS "Test File Pattern" (integer group array, `.tolist()`).
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3 && cargo fmt --check</automated>
    <human-check>tests/test_glmm.py executed by the Plan 06 phase gate (maturin+pytest)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "GLMM"' src/pymodels/py_glmm.rs`; struct field `fitted: Option<FittedGlmm>` present.
    - `grep -q 'fn fit_crossed' src/pymodels/py_glmm.rs` and `grep -q 'fn factors' src/pymodels/py_glmm.rs`.
    - `grep -q 'as usize' src/pymodels/py_glmm.rs` (group conversion present).
    - `tests/test_glmm.py` exists importing `from polars_statistics import GLMM`.
    - `cargo build --features python`, `cargo clippy --all-targets --features python -- -D warnings`, and `cargo fmt --check` all exit 0.
  </acceptance_criteria>
  <done>PyGLMM compiles clean, exposes gaussian/poisson/binomial factories, fit(x,y,group), fit_crossed, factors() list-of-dicts, and the FittedGlmm getters; smoke test file exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PyPSpline (single-column smoother)</name>
  <files>src/pymodels/py_pspline.rs, tests/test_pspline.py</files>
  <read_first>
    - .planning/phases/04-regression-api-parity/04-RESEARCH.md (section "REGR-02: PSplineRegressor + FittedPSpline" — construction `PSplineRegressor::new().with_n_basis().with_penalty_order().with_lambda()`, `fit` requires n×1 x, `FittedPSpline::edf()`/`sigma2()`, `result()` fields; AND "Pitfall 2" on the ncols==1 requirement)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "src/pymodels/py_pspline.rs" — ncols validation, method-chaining builder, edf/sigma2 getter excerpts)
    - src/pymodels/py_huber.rs (builder + fit/predict/is_fitted/getter structural analog)
    - src/pymodels/py_ridge.rs (builder-field construction analog)
  </read_first>
  <behavior>
    - Test: `PSpline().fit(X_1col, y)` returns `is_fitted()==True`, finite `edf`, and `predict()` shape (n,).
    - Test: `PSpline().fit(X_2col, y)` raises ValueError (single-column requirement).
    - Test: reading `edf` before fit raises.
  </behavior>
  <action>
    Create `PyPSpline` with `#[pyclass(name = "PSpline")]`. Fields: `n_basis: usize`,
    `penalty_order: usize`, `lambda: Option<f64>`, `fitted: Option<FittedPSpline>`. `#[new]` signature
    `(n_basis=0, penalty_order=2, lambda=None)` (n_basis=0 means automatic per RESEARCH). In `fit`,
    FIRST validate `x.shape()[1] == 1` and raise `PyValueError` "PSpline requires a single-column
    predictor matrix (n, 1)." otherwise (per RESEARCH Pitfall 2), then build via method chaining
    `PSplineRegressor::new().with_n_basis(..).with_penalty_order(..)`, conditionally
    `.with_lambda(l)` only when `lambda` is `Some`, then `.build()` and `fit`. Add getters `edf`
    (`fitted.edf()`), `sigma2` (`fitted.sigma2()`), `coefficients`, `r_squared`, `rmse` (from
    `fitted.result()` — note AIC/BIC are NOT set for PSpline, so do not expose them). `predict`
    delegates to `fitted.predict`. Write `tests/test_pspline.py` using a single-column X (e.g.
    `X = np.linspace(0,1,60).reshape(-1,1)`, `y = sin(2πX) + noise`), asserting `edf` finite and
    `predict` shape.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3 && cargo fmt --check</automated>
    <human-check>tests/test_pspline.py executed by the Plan 06 phase gate (maturin+pytest)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "PSpline"' src/pymodels/py_pspline.rs`; struct field `fitted: Option<FittedPSpline>` present.
    - `grep -q 'single-column' src/pymodels/py_pspline.rs` (ncols guard message present).
    - `grep -q 'fn edf' src/pymodels/py_pspline.rs` and `grep -q 'fn sigma2' src/pymodels/py_pspline.rs`.
    - `tests/test_pspline.py` exists importing `from polars_statistics import PSpline`.
    - `cargo build --features python`, `cargo clippy --all-targets --features python -- -D warnings`, and `cargo fmt --check` all exit 0.
  </acceptance_criteria>
  <done>PyPSpline compiles clean, validates single-column input, exposes edf/sigma2/predict, and the smoke test file exists.</done>
</task>

<task type="auto">
  <name>Task 3: Register GLMM and PSpline in the three shared files</name>
  <files>src/pymodels/mod.rs, src/lib.rs, python/polars_statistics/__init__.py</files>
  <read_first>
    - src/pymodels/mod.rs (post-Plan-01 state; add py_glmm and py_pspline mod + pub use among py_g*/py_p* neighbours)
    - src/lib.rs (#[pymodule] block; add PyGLMM under `// GLM Models`, PyPSpline under `// Robust Regression` or a new `// Smoothers` comment)
    - python/polars_statistics/__init__.py (import block + __all__)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "Shared Registration Files")
  </read_first>
  <action>
    This is the SINGLE registration task for Wave 2 (per RESEARCH Pitfall 5 — only one task per wave
    edits the shared files). Run it AFTER Tasks 1 and 2. In `src/pymodels/mod.rs` add
    `mod py_glmm;` + `mod py_pspline;` and `pub use py_glmm::PyGLMM;` + `pub use py_pspline::PyPSpline;`.
    In `src/lib.rs` add `m.add_class::<pymodels::PyGLMM>()?;` (under `// GLM Models`) and
    `m.add_class::<pymodels::PyPSpline>()?;` (under `// Robust Regression`, or add a `// Smoothers`
    comment). In `python/polars_statistics/__init__.py` add `GLMM` and `PSpline` to the
    `_polars_statistics` import block and to `__all__`. Use scoped Edit on each file.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'mod py_glmm;' src/pymodels/mod.rs` and `grep -q 'mod py_pspline;' src/pymodels/mod.rs`.
    - `grep -q 'PyGLMM' src/lib.rs` and `grep -q 'PyPSpline' src/lib.rs`.
    - `grep -q '"GLMM"' python/polars_statistics/__init__.py` and `grep -q '"PSpline"' python/polars_statistics/__init__.py`.
    - `cargo build --features python` exits 0.
  </acceptance_criteria>
  <done>GLMM and PSpline are declared, exported, added to the pymodule, and imported + listed in __all__; the workspace builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → PyO3 (`fit`/`fit_crossed`) | User numpy arrays + integer group arrays cross into Rust; lengths and shape not guaranteed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | GLMM group array length != n rows | low | mitigate | `GlmmRegressor::fit` validates internally and returns `Err`; mapped to `PyValueError` (no panic) |
| T-04-04 | Tampering | PSpline multi-column X silently ignored | low | mitigate | Explicit `ncols == 1` guard raises `PyValueError` before `to_faer()` (per RESEARCH Pitfall 2) |
| T-04-05 | Denial of Service | Degenerate design → GLMM non-convergence | low | accept | REML/PIRLS iteration cap in the crate returns after `max_iterations`; `converged` getter exposes status |
</threat_model>

<verification>
- All three cargo gates (build / clippy -D warnings / fmt --check) green.
- GLMM and PSpline importable and registered in all three files (grep checks).
- pytest smoke deferred to Plan 06 phase gate.
</verification>

<success_criteria>
- REGR-01 satisfied: GLMM PyModel with fit/fit_crossed/factors callable from Python.
- REGR-02 satisfied: PSpline smoother PyModel callable from Python.
</success_criteria>

<output>
Create `.planning/phases/04-regression-api-parity/04-02-SUMMARY.md` when done.
</output>
