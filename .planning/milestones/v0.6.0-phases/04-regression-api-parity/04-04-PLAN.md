---
phase: 04-regression-api-parity
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/pymodels/py_theil_sen.rs
  - src/pymodels/py_ransac.rs
  - src/pymodels/py_bayesian_ridge.rs
  - src/pymodels/py_ard.rs
  - src/pymodels/mod.rs
  - src/lib.rs
  - python/polars_statistics/__init__.py
  - tests/test_theil_sen.py
  - tests/test_ransac.py
  - tests/test_bayesian.py
autonomous: true
requirements: [REGR-06]
estimate:
  tokens: 88000
  raw_tokens: 46000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "User can `from polars_statistics import TheilSen, RANSAC, BayesianRidge, ARD` and fit each on a small design, getting finite coefficients (REGR-06)"
    - "`RANSAC().fit(X, y).inlier_mask` returns a boolean array of length n"
    - "`BayesianRidge().fit(X, y).alpha_` and `.lambda_` are finite; `ARD().fit(X, y).lambdas` has length n_features"
  artifacts:
    - src/pymodels/py_theil_sen.rs
    - src/pymodels/py_ransac.rs
    - src/pymodels/py_bayesian_ridge.rs
    - src/pymodels/py_ard.rs
    - tests/test_theil_sen.py
    - tests/test_ransac.py
    - tests/test_bayesian.py
  key_links:
    - "All four classes registered in mod.rs + lib.rs #[pymodule] + __init__.py"
    - "BayesianRidge/ARD use fit_intercept (not with_intercept); TheilSen/RANSAC use with_intercept — do not mix (RESEARCH anti-pattern)"
    - "TheilSen/RANSAC optional builder params (n_subsamples, min_samples, residual_threshold, stop_n_inliers) applied only when Some"
---

<objective>
Add the four sklearn-style MEDIUM-priority solvers (REGR-06): `TheilSen` and `RANSAC` (robust
regressors) and `BayesianRidge` and `ARD` (empirical-Bayes regressors). All are standard PyModel
classes following the `py_huber.rs`/`py_ridge.rs` analogs; they touch four independent new files and
share only the three registration files (edited once in Task 3).

Purpose: Deliver the bulk of the remaining regression gap in one parallel-friendly plan.
Output: four new PyModel files, registration edits, three smoke test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PyTheilSen and PyRANSAC (robust solvers)</name>
  <files>src/pymodels/py_theil_sen.rs, src/pymodels/py_ransac.rs, tests/test_theil_sen.py, tests/test_ransac.py</files>
  <read_first>
    - .planning/phases/04-regression-api-parity/04-RESEARCH.md (sections "REGR-06a: TheilSenRegressor" and "REGR-06b: RansacRegressor" — exact builder setters `with_intercept`/`max_subpopulation`/`n_subsamples`/`max_iter`/`tolerance`/`random_state` for TheilSen and `with_intercept`/`min_samples`/`residual_threshold`/`max_trials`/`stop_probability`/`stop_n_inliers`/`random_state` for RANSAC, `FittedTheilSen`/`FittedRansac` methods, `inlier_mask` on RANSAC, and the NaN-for-inference note)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (sections "src/pymodels/py_theil_sen.rs" and "src/pymodels/py_ransac.rs" — struct fields, optional-param builder pattern, and the `inlier_mask` PyArray1<bool> getter excerpt)
    - src/pymodels/py_huber.rs (exact structural analog — imports, struct, #[new], fit/predict/is_fitted/getters)
  </read_first>
  <behavior>
    - Test: `TheilSen().fit(X, y)` finite coefficients, `predict(X)` shape (n,).
    - Test: `RANSAC().fit(X, y).inlier_mask` has length n and dtype bool.
    - Test: reading a getter before fit raises.
  </behavior>
  <action>
    Create `PyTheilSen` (`#[pyclass(name = "TheilSen")]`) and `PyRANSAC` (`#[pyclass(name = "RANSAC")]`)
    mirroring `src/pymodels/py_huber.rs`. Use the exact builder setters and defaults from RESEARCH:
    TheilSen `#[new] (with_intercept=true, max_subpopulation=10000, n_subsamples=None, max_iter=300,
    tol=1e-3, random_state=0)`; RANSAC `#[new] (with_intercept=true, min_samples=None,
    residual_threshold=None, max_trials=100, stop_probability=0.99, stop_n_inliers=None,
    random_state=0)`. Apply optional builder params only when `Some` (per 04-PATTERNS optional-param
    pattern). Both use `with_intercept` (NOT `fit_intercept`). Getters: `coefficients`, `intercept`,
    `r_squared`, `residuals` from `result()` (expose NaN inference fields as-is; do not omit).
    RANSAC additionally exposes `inlier_mask` (via `PyArray1::from_slice(py, fitted.inlier_mask())`),
    `n_inliers`, `n_trials`, `residual_threshold`. Write `tests/test_theil_sen.py` and
    `tests/test_ransac.py` per the smoke-test template; the RANSAC test asserts `inlier_mask` length.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3 && cargo fmt --check</automated>
    <human-check>tests executed by the Plan 06 phase gate (maturin+pytest)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "TheilSen"' src/pymodels/py_theil_sen.rs` and `grep -q 'name = "RANSAC"' src/pymodels/py_ransac.rs`.
    - `grep -q 'inlier_mask' src/pymodels/py_ransac.rs`.
    - `grep -q 'with_intercept' src/pymodels/py_theil_sen.rs` (NOT fit_intercept).
    - `tests/test_theil_sen.py` and `tests/test_ransac.py` exist.
    - `cargo build --features python`, `cargo clippy --all-targets --features python -- -D warnings`, `cargo fmt --check` all exit 0.
  </acceptance_criteria>
  <done>PyTheilSen and PyRANSAC compile clean with correct builder params and (RANSAC) inlier_mask; smoke tests exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PyBayesianRidge and PyARD (empirical-Bayes solvers)</name>
  <files>src/pymodels/py_bayesian_ridge.rs, src/pymodels/py_ard.rs, tests/test_bayesian.py</files>
  <read_first>
    - .planning/phases/04-regression-api-parity/04-RESEARCH.md (section "REGR-06c: BayesianRidge + ArdRegression" — builder setters using `fit_intercept`/`max_iter`/`tolerance`/`alpha_1`/`alpha_2`/`lambda_1`/`lambda_2`/`alpha_init`/`lambda_init` (+ `threshold_lambda` for ARD), `FittedBayesianRidge::alpha()`/`lambda()`/`sigma_diag()`, `FittedArd::alpha()`/`lambdas()`; AND the anti-pattern note that these use `fit_intercept` NOT `with_intercept`)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "src/pymodels/py_bayesian_ridge.rs + src/pymodels/py_ard.rs" — struct, #[new] signature with fit_intercept, and the alpha_/lambda_/sigma_diag/lambdas getter excerpts)
    - src/pymodels/py_huber.rs (structural analog) and src/pymodels/py_ridge.rs (fit_intercept parameter-name style)
  </read_first>
  <behavior>
    - Test: `BayesianRidge().fit(X, y)` finite coefficients; `.alpha_` and `.lambda_` finite.
    - Test: `ARD().fit(X, y).lambdas` has length == n_features.
    - Test: reading a getter before fit raises.
  </behavior>
  <action>
    Create `PyBayesianRidge` (`#[pyclass(name = "BayesianRidge")]`) and `PyARD`
    (`#[pyclass(name = "ARD")]`) mirroring `py_huber.rs`/`py_ridge.rs`. `#[new]` for BayesianRidge:
    `(fit_intercept=true, max_iter=300, tol=1e-3, alpha_1=1e-6, alpha_2=1e-6, lambda_1=1e-6,
    lambda_2=1e-6, alpha_init=None, lambda_init=None)`; ARD adds `threshold_lambda=10000.0`. Use the
    `fit_intercept` setter (NOT `with_intercept`) and apply `alpha_init`/`lambda_init` only when
    `Some`. Getters: `coefficients`, `intercept`, `r_squared` from `result()`; BayesianRidge adds
    `alpha_` (`fitted.alpha()`), `lambda_` (`fitted.lambda()`), `sigma_diag`
    (`PyArray1::from_slice(py, fitted.sigma_diag())`); ARD adds `alpha_` and `lambdas`
    (`PyArray1::from_slice(py, fitted.lambdas())`). Write `tests/test_bayesian.py` covering BOTH
    classes (a `TestBayesianRidge` and a `test_ard`), asserting the hyperparameter getters.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3 && cargo fmt --check</automated>
    <human-check>tests/test_bayesian.py executed by the Plan 06 phase gate (maturin+pytest)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "BayesianRidge"' src/pymodels/py_bayesian_ridge.rs` and `grep -q 'name = "ARD"' src/pymodels/py_ard.rs`.
    - `grep -q 'fit_intercept' src/pymodels/py_bayesian_ridge.rs` (NOT with_intercept).
    - `grep -q 'fn lambdas' src/pymodels/py_ard.rs` and `grep -q 'threshold_lambda' src/pymodels/py_ard.rs`.
    - `tests/test_bayesian.py` exists and references both `BayesianRidge` and `ARD`.
    - `cargo build --features python`, `cargo clippy --all-targets --features python -- -D warnings`, `cargo fmt --check` all exit 0.
  </acceptance_criteria>
  <done>PyBayesianRidge and PyARD compile clean using fit_intercept and expose their hyperparameter getters; combined smoke test exists.</done>
</task>

<task type="auto">
  <name>Task 3: Register TheilSen, RANSAC, BayesianRidge, ARD in the three shared files</name>
  <files>src/pymodels/mod.rs, src/lib.rs, python/polars_statistics/__init__.py</files>
  <read_first>
    - src/pymodels/mod.rs (add four mod + four pub use lines)
    - src/lib.rs (#[pymodule] block — add a `// Robust & Sklearn-Style Solvers` comment section after the existing `// Robust Regression` block per 04-PATTERNS, and add the four add_class lines)
    - python/polars_statistics/__init__.py (import block + __all__)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "Shared Registration Files")
  </read_first>
  <action>
    SINGLE Wave-4 registration task (per Pitfall 5). Run AFTER Tasks 1 and 2. In `src/pymodels/mod.rs`
    add `mod py_theil_sen; mod py_ransac; mod py_bayesian_ridge; mod py_ard;` and the four
    `pub use py_*::Py*;` exports (PyTheilSen, PyRANSAC, PyBayesianRidge, PyARD). In `src/lib.rs` add a
    `// Robust & Sklearn-Style Solvers` comment and the four `m.add_class::<pymodels::Py*>()?;` lines.
    In `python/polars_statistics/__init__.py` add `TheilSen`, `RANSAC`, `BayesianRidge`, `ARD` to the
    `_polars_statistics` import block and to `__all__`. Scoped Edit on each file.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PyTheilSen\|PyRANSAC\|PyBayesianRidge\|PyARD' src/pymodels/mod.rs` is >= 8 (4 mod-derived + 4 pub use).
    - `grep -c 'PyTheilSen\|PyRANSAC\|PyBayesianRidge\|PyARD' src/lib.rs` is >= 4.
    - `grep -q '"TheilSen"' python/polars_statistics/__init__.py` and `grep -q '"RANSAC"' python/polars_statistics/__init__.py` and `grep -q '"BayesianRidge"' python/polars_statistics/__init__.py` and `grep -q '"ARD"' python/polars_statistics/__init__.py`.
    - `cargo build --features python` exits 0.
  </acceptance_criteria>
  <done>All four solvers are declared, exported, added to the pymodule, and imported + listed in __all__; the workspace builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → PyO3 (`fit`) | User numpy f64 arrays cross into robust/Bayes solvers; degenerate designs possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Denial of Service | RANSAC fails to find a consensus set | low | mitigate | `RansacRegressor::fit` returns `ConvergenceFailed`; mapped to `PyValueError` (no panic) |
| T-04-10 | Denial of Service | BayesianRidge/ARD on singular design | low | mitigate | SVD/LLT path returns `SingularMatrix` error; mapped to `PyValueError` |
| T-04-11 | Denial of Service | TheilSen large `max_subpopulation` → heavy compute | low | accept | Bounded by `max_subpopulation` default 10000; user-tunable |
</threat_model>

<verification>
- All three cargo gates green.
- Four classes importable and registered in all three files (grep checks).
- pytest smoke deferred to Plan 06 phase gate.
</verification>

<success_criteria>
- REGR-06 (partial): TheilSen, RANSAC, BayesianRidge, ARD callable from Python.
</success_criteria>

<output>
Create `.planning/phases/04-regression-api-parity/04-04-SUMMARY.md` when done.
</output>
