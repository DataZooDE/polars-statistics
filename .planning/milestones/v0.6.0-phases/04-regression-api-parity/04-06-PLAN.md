---
phase: 04-regression-api-parity
plan: 06
type: execute
wave: 6
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - tests/test_gamma.py
  - tests/test_glmm.py
  - tests/test_pspline.py
  - tests/test_theil_sen.py
  - tests/test_ransac.py
  - tests/test_bayesian.py
  - tests/test_lars.py
  - tests/test_pa.py
  - tests/test_moments.py
  - tests/test_glm_diagnostics.py
autonomous: true
requirements: [REGR-01, REGR-02, REGR-03, REGR-04, REGR-05, REGR-06]
estimate:
  tokens: 45000
  raw_tokens: 26000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Every new PyModel class (Gamma, GLMM, PSpline, TheilSen, RANSAC, BayesianRidge, ARD, LARS, PassiveAggressive, MomentAccumulator) is importable from polars_statistics and passes its smoke test"
    - "Every new GLM diagnostic expression is importable and returns finite output"
    - "The full Rust + pytest suite passes with clippy -D warnings, cargo fmt, and ruff green"
  artifacts:
    - tests/test_gamma.py
    - tests/test_glm_diagnostics.py
  key_links:
    - "maturin develop --features python must rebuild the extension so all newly registered classes/expressions are importable before pytest"
---

<objective>
Phase gate for Phase 4: run the definitive build + full test suite that validates every new
regression capability end-to-end. This plan owns the `maturin develop && pytest` execution that the
per-model plans deliberately deferred (that command stalls executor subagents, so the orchestrator
phase gate runs it). It also runs the CI-parity gates (clippy `-D warnings`, `cargo fmt --check`,
`ruff check`) to guarantee the merge gates stay green.

Purpose: Prove the whole phase's parity surface is callable and the CI gates pass before verification.
Output: green full suite; any smoke-test fixups discovered at real-execution time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-regression-api-parity/04-01-SUMMARY.md
@.planning/phases/04-regression-api-parity/04-02-SUMMARY.md
@.planning/phases/04-regression-api-parity/04-03-SUMMARY.md
@.planning/phases/04-regression-api-parity/04-04-SUMMARY.md
@.planning/phases/04-regression-api-parity/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI-parity static gates (clippy, fmt, ruff)</name>
  <files>src/pymodels/mod.rs, src/lib.rs, python/polars_statistics/__init__.py</files>
  <read_first>
    - .claude/CLAUDE.md (CI gate definitions: clippy `-D warnings`, cargo fmt, ruff line length 100 / py39 target / rules E,F,W,I,UP,B,C4 ignoring E501)
    - .planning/phases/04-regression-api-parity/04-05-SUMMARY.md (final registration state to confirm all 10 classes + 5 expressions are wired)
  </read_first>
  <action>
    Run the three static CI-parity gates and fix any residual issues introduced across Plans 01-05:
    `cargo clippy --all-targets --features python -- -D warnings`, `cargo fmt --check` (run
    `cargo fmt` to fix if it reports diffs), and `ruff check python/`. Fix any clippy warnings, unformatted
    Rust, or ruff violations (respecting line-length 100 / py39 target / the E501-ignore config).
    Confirm all ten new class names and five new expression names appear exactly once in
    `python/polars_statistics/__init__.py` `__all__` (no duplicates from the per-wave registration edits).
  </action>
  <verify>
    <automated>cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3 && cargo fmt --check && ruff check python/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy --all-targets --features python -- -D warnings` exits 0.
    - `cargo fmt --check` exits 0.
    - `ruff check python/` exits 0.
    - `grep -c '"Gamma"' python/polars_statistics/__init__.py` equals 1 (no duplicate __all__ entry); same for the other nine class names.
  </acceptance_criteria>
  <done>All static CI-parity gates pass and there are no duplicate registrations.</done>
</task>

<task type="auto">
  <name>Task 2: Build the extension and run the full smoke + regression suite (phase gate)</name>
  <files>tests/test_gamma.py, tests/test_glmm.py, tests/test_pspline.py, tests/test_theil_sen.py, tests/test_ransac.py, tests/test_bayesian.py, tests/test_lars.py, tests/test_pa.py, tests/test_moments.py, tests/test_glm_diagnostics.py</files>
  <read_first>
    - .planning/phases/04-regression-api-parity/04-RESEARCH.md (section "Validation Architecture" — the Phase Requirements → Test Map and the full-suite command `maturin develop --features python && pytest tests/ -v --tb=short`)
    - all ten new tests/test_*.py smoke files (to fix any mismatch between the test's expected API and the implemented getters)
  </read_first>
  <action>
    This is the definitive maturin+pytest phase gate — the orchestrator (not a stalled executor
    subagent) runs it. Run `maturin develop --features python` to rebuild the extension so all newly
    registered classes and expressions are importable, then run the full suite
    `pytest tests/ -v --tb=short`. Confirm: (a) each of the ten new smoke tests passes, proving every
    new class/expression is importable and callable (REGR-01..06); (b) the pre-existing suite still
    passes (no regressions). If any smoke test fails due to a mismatch between the test's expected
    API and an implemented getter/method name, fix the test file to match the actually-implemented
    API from the corresponding SUMMARY (do NOT change semantics — align names/shapes only). Do not
    weaken assertions to force a pass.
  </action>
  <verify>
    <automated>maturin develop --features python 2>&1 | tail -3 && python -m pytest tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `maturin develop --features python` completes successfully (extension rebuilt).
    - `python -m pytest tests/ -q` exits 0 (full suite green, including all ten new smoke files).
    - `python -c "from polars_statistics import Gamma, GLMM, PSpline, TheilSen, RANSAC, BayesianRidge, ARD, LARS, PassiveAggressive, MomentAccumulator; print('OK')"` prints OK.
  </acceptance_criteria>
  <done>The extension builds via maturin, all new classes/expressions import, and the full pytest suite passes with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build/test harness | No new trust boundary; this plan runs gates over code shipped by Plans 01-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Tampering | Duplicate/partial registration hides an unimportable class | low | mitigate | Explicit import-all + per-class smoke test surfaces any missing registration before verification |
</threat_model>

<verification>
- clippy `-D warnings`, `cargo fmt --check`, `ruff check python/` all exit 0.
- `maturin develop --features python` succeeds and the full pytest suite is green.
- All ten new classes import in a single statement.
</verification>

<success_criteria>
- All six REGR requirements are demonstrably callable from Python and covered by a passing smoke test.
- CI-parity gates (clippy/fmt/ruff) and the full test suite are green — ready for /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/04-regression-api-parity/04-06-SUMMARY.md` when done.
</output>
