---
phase: 04-regression-api-parity
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - src/pymodels/py_lars.rs
  - src/pymodels/py_passive_aggressive.rs
  - src/pymodels/py_moment_accumulator.rs
  - src/pymodels/py_ols.rs
  - src/pymodels/py_ridge.rs
  - src/pymodels/mod.rs
  - src/lib.rs
  - python/polars_statistics/__init__.py
  - tests/test_lars.py
  - tests/test_pa.py
  - tests/test_moments.py
autonomous: true
requirements: [REGR-06]
estimate:
  tokens: 90000
  raw_tokens: 48000
  tasks: 4
  confidence: med
must_haves:
  truths:
    - "User can `from polars_statistics import LARS` and read a non-empty `alphas` regularization path after fit (REGR-06)"
    - "User can `from polars_statistics import PassiveAggressive`, fit in batch, and stream via `partial_fit(x_row, y)` (REGR-06)"
    - "User can `from polars_statistics import MomentAccumulator`, `push_row(x, y)`, then `OLS().fit_from_accumulator(acc)` / `Ridge().fit_from_accumulator(acc)` to get finite coefficients (REGR-06)"
  artifacts:
    - src/pymodels/py_lars.rs
    - src/pymodels/py_passive_aggressive.rs
    - src/pymodels/py_moment_accumulator.rs
    - tests/test_lars.py
    - tests/test_pa.py
    - tests/test_moments.py
  key_links:
    - "MomentAccumulator.push_row takes &[f64] via x_row.as_slice(), NOT to_faer() (RESEARCH anti-pattern)"
    - "OLS/Ridge fit_from_accumulator method name must be verified against src/solvers/ols.rs at implementation time (RESEARCH Open Question 1)"
    - "LarsMethod and PaLoss are string-dispatched, never registered as #[pyclass] enums"
    - "All three new classes registered in mod.rs + lib.rs + __init__.py"
---

<objective>
Add the three PyModel-only streaming/path models (REGR-06): `LARS` (path-based regularization),
`PassiveAggressive` (online learner with `partial_fit`), and `MomentAccumulator` (streaming
sufficient-statistics utility). Also extend `PyOLS` and `PyRidge` with `fit_from_accumulator` so the
accumulator is usable. These are the most novel Python APIs in the phase.

Purpose: Close the final regression-parity gaps (streaming + online + path solvers).
Output: three new PyModel files, fit_from_accumulator on OLS/Ridge, registration edits, three smoke tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PyLARS (path-based) and PyPassiveAggressive (streaming)</name>
  <files>src/pymodels/py_lars.rs, src/pymodels/py_passive_aggressive.rs, tests/test_lars.py, tests/test_pa.py</files>
  <read_first>
    - .planning/phases/04-regression-api-parity/04-RESEARCH.md (sections "REGR-06d: LarsRegressor + LarsMethod" — `fit_intercept`, `method`, `n_nonzero_coefs`, `alpha`, `standardize`, `eps` builder params, `FittedLars::alphas()` (coefs_path is private), and "REGR-06e: PassiveAggressiveRegressor + PaState" — `c`/`epsilon`/`with_intercept`/`max_iter`/`tolerance`/`shuffle`/`loss`/`random_state` params, `partial_fit(state, x_row, y)`, `PaState::new(n_features)`, `FittedPassiveAggressive::n_iter()`; AND Pitfall 7 (LarsMethod string dispatch) and A5 (PaLoss string dispatch))
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (sections "src/pymodels/py_lars.rs" and "src/pymodels/py_passive_aggressive.rs" — struct with optional PaState, LarsMethod/PaLoss string dispatch, alphas getter, and partial_fit method excerpts)
    - src/pymodels/py_elastic_net.rs (alpha/method string-param analog for LARS)
    - src/pymodels/py_huber.rs (batch fit analog for PassiveAggressive)
    - src/pymodels/py_ols.rs (lines 95-101 — the SolverType string-dispatch pattern to mirror for LarsMethod/PaLoss)
  </read_first>
  <behavior>
    - Test: `LARS().fit(X, y).alphas` is non-empty and finite; `predict(X)` shape (n,).
    - Test: `PassiveAggressive().fit(X, y)` then `predict(X)` shape (n,); `partial_fit(X[0], y[0])` succeeds.
    - Test: reading a getter before fit raises.
  </behavior>
  <action>
    Create `PyLARS` (`#[pyclass(name = "LARS")]`) mirroring `py_elastic_net.rs`. Fields include a
    `method: String` ("lar"/"lasso") string-dispatched to `LarsMethod` (per Pitfall 7 — never register
    the enum), `fit_intercept`, `n_nonzero_coefs: Option<usize>`, `alpha`, `standardize`, `eps`.
    `#[new] (method="lar", fit_intercept=true, n_nonzero_coefs=None, alpha=0.0, standardize=false)`.
    Getters: `coefficients`, `intercept`, `r_squared` from `result()`, plus `alphas`
    (`PyArray1::from_slice(py, fitted.alphas())`). Do NOT expose coefs_path (private).
    Create `PyPassiveAggressive` (`#[pyclass(name = "PassiveAggressive")]`) mirroring `py_huber.rs`
    for batch `fit`, with fields `c`, `epsilon`, `with_intercept`, `max_iter`, `tol`, `shuffle`,
    `loss: String` (string-dispatched to `PaLoss`), `random_state`, `fitted: Option<FittedPassiveAggressive>`,
    and `state: Option<PaState>`. `#[new] (c=1.0, epsilon=0.1, with_intercept=true, max_iter=1000,
    tol=1e-3, shuffle=true, loss="epsilon_insensitive", random_state=0)`. Add a `partial_fit(&mut self,
    x_row: PyReadonlyArray1<f64>, y_value: f64)` method that lazily inits `state` via
    `PaState::new(n_features)`, builds the model from fields, and calls
    `model.partial_fit(state, x_row.as_slice().unwrap(), y_value)` mapping errors to `PyValueError`.
    Expose `n_iter` getter. Write `tests/test_lars.py` (assert alphas non-empty) and `tests/test_pa.py`
    (batch fit + one partial_fit call).
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3 && cargo fmt --check</automated>
    <human-check>tests executed by the Plan 06 phase gate (maturin+pytest)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "LARS"' src/pymodels/py_lars.rs` and `grep -q 'fn alphas' src/pymodels/py_lars.rs`.
    - `grep -q 'name = "PassiveAggressive"' src/pymodels/py_passive_aggressive.rs` and `grep -q 'fn partial_fit' src/pymodels/py_passive_aggressive.rs`.
    - `grep -q 'PaState' src/pymodels/py_passive_aggressive.rs` and `grep -q 'as_slice' src/pymodels/py_passive_aggressive.rs`.
    - `tests/test_lars.py` and `tests/test_pa.py` exist.
    - `cargo build --features python`, `cargo clippy --all-targets --features python -- -D warnings`, `cargo fmt --check` all exit 0.
  </acceptance_criteria>
  <done>PyLARS and PyPassiveAggressive compile clean with string-dispatched enums, alphas getter, and partial_fit; smoke tests exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PyMomentAccumulator and add fit_from_accumulator to PyOLS/PyRidge</name>
  <files>src/pymodels/py_moment_accumulator.rs, src/pymodels/py_ols.rs, src/pymodels/py_ridge.rs, tests/test_moments.py</files>
  <read_first>
    - .planning/phases/04-regression-api-parity/04-RESEARCH.md (section "REGR-06f: MomentAccumulator" — `new(n_features)`, `push_row(x_row: &[f64], y)`, `merge`, `clear`, getters `n`/`n_features`/`sum_x`/`sum_y`/`xtx`/`xty`, and the note that `OlsRegressor`/`RidgeRegressor` gain `fit_from_accumulator`; AND Pitfall 4 (push_row arity), the anti-pattern on using to_faer for push_row, and Open Question 1 (verify the exact fit_from_accumulator method name against src/solvers/ols.rs))
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "src/pymodels/py_moment_accumulator.rs" — struct wrapping `inner: MomentAccumulator`, push_row/merge/clear/getters, and the fit_from_accumulator addition to PyOLS/PyRidge excerpt)
    - src/pymodels/py_ols.rs (existing #[pymethods] block, the OlsRegressor builder construction, and how `fitted` is set — mirror for fit_from_accumulator)
    - src/pymodels/py_ridge.rs (existing #[pymethods] block and RidgeRegressor construction)
  </read_first>
  <behavior>
    - Test: `MomentAccumulator(2).push_row(X[i], y[i])` in a loop, then `OLS().fit_from_accumulator(acc)` gives finite coefficients close to a direct OLS fit.
    - Test: `Ridge().fit_from_accumulator(acc)` gives finite coefficients.
    - Test: `push_row` with a wrong-length row raises ValueError.
  </behavior>
  <action>
    FIRST, read `~/.cargo/registry/.../anofox-regression-0.5.13/src/solvers/ols.rs` (first ~120 lines)
    to confirm the exact method name (`fit_from_accumulator` vs `fit_from_moments`) and signature
    before writing the OLS/Ridge methods (RESEARCH Open Question 1 / assumption A4). Create
    `PyMomentAccumulator` (`#[pyclass(name = "MomentAccumulator")]`) wrapping
    `inner: MomentAccumulator`. `#[new] (n_features)`. Methods: `push_row(&mut self, x_row:
    PyReadonlyArray1<f64>, y: f64)` — validate `x_row.len() == self.inner.n_features()` and raise
    `PyValueError` on mismatch (Pitfall 4), then call `self.inner.push_row(x_row.as_slice().unwrap(),
    y)` (use as_slice, NOT to_faer — anti-pattern) mapping errors to `PyValueError`; `merge(&mut self,
    other: &PyMomentAccumulator)`; `clear(&mut self)`. Getters: `n`, `n_features`, `sum_y` (scalars),
    `sum_x` (Col→numpy via IntoNumpy), `xtx` (Mat→2D numpy), `xty`. Then add
    `fit_from_accumulator(mut slf: PyRefMut<Self>, acc: &PyMomentAccumulator)` methods to the existing
    `PyOLS` and `PyRidge` #[pymethods] blocks, building the respective regressor and calling the
    verified accumulator-fit method with `&acc.inner`, mapping errors to `PyValueError` and setting
    `slf.fitted = Some(...)`. Write `tests/test_moments.py` per the MomentAccumulator deviation
    template in 04-PATTERNS.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3 && cargo fmt --check</automated>
    <human-check>tests/test_moments.py executed by the Plan 06 phase gate (maturin+pytest)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "MomentAccumulator"' src/pymodels/py_moment_accumulator.rs` and `grep -q 'fn push_row' src/pymodels/py_moment_accumulator.rs`.
    - `grep -q 'as_slice' src/pymodels/py_moment_accumulator.rs` (NOT to_faer for push_row).
    - `grep -q 'fn fit_from_accumulator' src/pymodels/py_ols.rs` and `grep -q 'fn fit_from_accumulator' src/pymodels/py_ridge.rs`.
    - `tests/test_moments.py` exists referencing `MomentAccumulator` and `fit_from_accumulator`.
    - `cargo build --features python`, `cargo clippy --all-targets --features python -- -D warnings`, `cargo fmt --check` all exit 0.
  </acceptance_criteria>
  <done>PyMomentAccumulator compiles clean; PyOLS and PyRidge gain fit_from_accumulator; smoke test exists.</done>
</task>

<task type="auto">
  <name>Task 3: Register LARS, PassiveAggressive, MomentAccumulator in the three shared files</name>
  <files>src/pymodels/mod.rs, src/lib.rs, python/polars_statistics/__init__.py</files>
  <read_first>
    - src/pymodels/mod.rs (add three mod + three pub use lines)
    - src/lib.rs (#[pymodule] block — add the three add_class lines under the `// Robust & Sklearn-Style Solvers` section created in Plan 04, or a `// Streaming & Online` comment)
    - python/polars_statistics/__init__.py (import block + __all__)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "Shared Registration Files")
  </read_first>
  <action>
    SINGLE Wave-5 registration task (per Pitfall 5). Run AFTER Tasks 1 and 2. In `src/pymodels/mod.rs`
    add `mod py_lars; mod py_passive_aggressive; mod py_moment_accumulator;` and the three
    `pub use py_*::Py*;` exports (PyLARS, PyPassiveAggressive, PyMomentAccumulator). In `src/lib.rs`
    add the three `m.add_class::<pymodels::Py*>()?;` lines under a `// Streaming & Online` comment.
    In `python/polars_statistics/__init__.py` add `LARS`, `PassiveAggressive`, `MomentAccumulator` to
    the import block and `__all__`. Scoped Edit on each file.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'mod py_lars;' src/pymodels/mod.rs` and `grep -q 'mod py_passive_aggressive;' src/pymodels/mod.rs` and `grep -q 'mod py_moment_accumulator;' src/pymodels/mod.rs`.
    - `grep -c 'PyLARS\|PyPassiveAggressive\|PyMomentAccumulator' src/lib.rs` is >= 3.
    - `grep -q '"LARS"' python/polars_statistics/__init__.py` and `grep -q '"PassiveAggressive"' python/polars_statistics/__init__.py` and `grep -q '"MomentAccumulator"' python/polars_statistics/__init__.py`.
    - `cargo build --features python` exits 0.
  </acceptance_criteria>
  <done>LARS, PassiveAggressive, and MomentAccumulator are declared, exported, added to the pymodule, and imported + listed in __all__; the workspace builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → PyO3 (`fit`/`partial_fit`/`push_row`) | User numpy arrays cross into streaming/online solvers; per-row arity not guaranteed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Tampering | `push_row` with wrong-length row | low | mitigate | Explicit length check raises `PyValueError` before `push_row` (Pitfall 4) |
| T-04-13 | Denial of Service | LARS empty coefficient path panics | low | mitigate | Crate validates the path; fit error mapped to `PyValueError`; only public `alphas()` exposed |
| T-04-14 | Denial of Service | `MomentAccumulator::new(p)` huge p → OOM on p×p alloc | low | accept | faer allocation panics on OOM rather than UB; p is user-controlled and bounded by intent |
</threat_model>

<verification>
- All three cargo gates green.
- Three classes importable + registered; fit_from_accumulator present on OLS/Ridge (grep checks).
- pytest smoke deferred to Plan 06 phase gate.
</verification>

<success_criteria>
- REGR-06 (complete): LARS, PassiveAggressive, MomentAccumulator callable; OLS/Ridge accept accumulators.
</success_criteria>

<output>
Create `.planning/phases/04-regression-api-parity/04-05-SUMMARY.md` when done.
</output>
