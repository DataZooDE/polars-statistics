---
phase: 04-regression-api-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pymodels/py_gamma.rs
  - src/pymodels/mod.rs
  - src/lib.rs
  - python/polars_statistics/__init__.py
  - tests/test_gamma.py
autonomous: true
requirements: [REGR-03]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "User can `from polars_statistics import Gamma`, call `Gamma().fit(X, y)`, and get finite coefficients (REGR-03)"
    - "`Gamma().fit(X, y).predict(X)` returns an array of length n on the response (mu) scale"
    - "`Gamma().converged` reports IRLS convergence status"
  artifacts:
    - src/pymodels/py_gamma.rs
    - tests/test_gamma.py
  key_links:
    - "PyGamma registered in src/pymodels/mod.rs (mod + pub use), src/lib.rs #[pymodule] (add_class), and python/polars_statistics/__init__.py (import + __all__) — all three or the class is not importable"
---

<objective>
Tracer for Phase 4: wire `GammaRegressor` (REGR-03) end-to-end as the `Gamma` PyModel class —
implemented, registered in all three shared registration points, and proven callable via a smoke
test. `GammaRegressor` is a thin re-skin of the already-exposed `TweedieRegressor`, so this is the
simplest new model and establishes the exact PyModel-add + registration pattern that every later
Phase 4 plan follows.

Purpose: Prove the full add-a-PyModel path (implement → register in 3 files → import from Python)
on the best early-context tokens before expanding to the harder GLMM/PSpline/solver models.
Output: `src/pymodels/py_gamma.rs`, registration edits, `tests/test_gamma.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Implement PyGamma end-to-end (fit/predict/getters)</name>
  <files>src/pymodels/py_gamma.rs, tests/test_gamma.py</files>
  <read_first>
    - .planning/phases/04-regression-api-parity/04-RESEARCH.md (section "REGR-03: GammaRegressor" — exact `GammaRegressor::builder()` setters `with_intercept`/`max_iterations`/`tolerance`/`lambda`, `FittedGamma` methods `predict_mu`/`predict_eta`/`converged`, and `result()` fields)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "src/pymodels/py_gamma.rs" — struct/#[new]/fit/predict/getter code excerpts with line refs to py_tweedie.rs)
    - src/pymodels/py_tweedie.rs (the exact structural analog — mirror its imports, struct, #[new], fit, predict, and getters verbatim, swapping Tweedie→Gamma types)
  </read_first>
  <behavior>
    - Test: `Gamma().fit(X, y)` on a 50x2 positive-response design returns `is_fitted()==True` and `len(coefficients)==2`, all finite.
    - Test: `Gamma().fit(X, y).predict(X)` has shape (n,) and is all finite.
    - Test: accessing `.coefficients` before fit raises an exception.
    - Test: `.converged` is True on a well-conditioned fit.
  </behavior>
  <action>
    Create `PyGamma` with `#[pyclass(name = "Gamma")]` mirroring `src/pymodels/py_tweedie.rs`.
    Import `FittedGamma`, `GammaRegressor`, `Regressor`, `FittedRegressor` from
    `anofox_regression::solvers`, and `IntoNumpy`/`ToFaer` from `crate::utils`. Private fields:
    `with_intercept: bool`, `max_iter: usize`, `tol: f64`, `lambda_: f64`, `fitted: Option<FittedGamma>`.
    `#[new]` signature `(with_intercept=true, max_iter=25, tol=1e-8, lambda_=0.0)` matching the crate
    builder defaults per RESEARCH. In `fit`, build via
    `GammaRegressor::builder().with_intercept(..).max_iterations(..).tolerance(..).lambda(..).build()`
    (note: setter is `max_iterations` and `lambda`, NOT `max_iter`/`lambda_`), map fit errors to
    `PyValueError`. `predict` delegates to `fitted.predict(&x.to_faer())` (response/mu scale). Add
    getters `coefficients`, `intercept`, `std_errors`, `p_values`, `aic`, `bic` reading
    `fitted.result()` fields (per RESEARCH `RegressionResult` field list), plus Gamma-specific
    `converged` (from `fitted.converged()`) and a `predict_eta` method (from `fitted.predict_eta`).
    Every getter guards the unfitted case with `PyRuntimeError` "Model not fitted". Do NOT invent
    fields not present in `RegressionResult`. Also write `tests/test_gamma.py` per the smoke-test
    template in 04-PATTERNS.md ("Test File Pattern"), using a strictly-positive `y`
    (e.g. `np.exp(X @ beta + noise)`) since Gamma requires positive responses.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3 && cargo fmt --check</automated>
    <human-check>tests/test_gamma.py executed by the phase gate via `maturin develop --features python && pytest tests/test_gamma.py -x` (run in Plan 06, not here — maturin/pytest stalls executor subagents)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name = "Gamma"' src/pymodels/py_gamma.rs` and the struct field `fitted: Option<FittedGamma>` present.
    - `grep -q 'GammaRegressor::builder()' src/pymodels/py_gamma.rs` and `grep -q 'predict_eta' src/pymodels/py_gamma.rs` and `grep -q 'converged' src/pymodels/py_gamma.rs`.
    - `tests/test_gamma.py` exists and imports `from polars_statistics import Gamma`.
    - `cargo build --features python` exits 0.
    - `cargo clippy --all-targets --features python -- -D warnings` exits 0.
    - `cargo fmt --check` exits 0.
  </acceptance_criteria>
  <done>PyGamma compiles clean under clippy, exposes fit/predict/getters/converged/predict_eta, and the smoke test file exists importing `Gamma`.</done>
</task>

<task type="auto">
  <name>Task 2: Register Gamma in all three shared registration files</name>
  <files>src/pymodels/mod.rs, src/lib.rs, python/polars_statistics/__init__.py</files>
  <read_first>
    - src/pymodels/mod.rs (regression `mod` section lines 3-24 and `pub use` export section lines 38-59 — add alphabetically among the `py_g*` neighbours)
    - src/lib.rs (the `#[pymodule]` block; the `// GLM Models` comment is at line 45 — add PyGamma there next to PyTweedie)
    - python/polars_statistics/__init__.py (the `_polars_statistics` import block and the `__all__` list — mirror how `Tweedie` is imported and listed)
    - .planning/phases/04-regression-api-parity/04-PATTERNS.md (section "Shared Registration Files" — exact edit pattern for each of the three files)
  </read_first>
  <action>
    In `src/pymodels/mod.rs` add `mod py_gamma;` in the "Regression models" mod section and
    `pub use py_gamma::PyGamma;` in the "Regression model exports" section. In `src/lib.rs` add
    `m.add_class::<pymodels::PyGamma>()?;` under the `// GLM Models` comment (per D-none; follows the
    crate's Gamma-as-GLM classification). In `python/polars_statistics/__init__.py` add `Gamma` to
    the `from polars_statistics._polars_statistics import (...)` block and add `"Gamma"` to `__all__`.
    Use Edit (scoped) on each file — do NOT rewrite the files.
  </action>
  <verify>
    <automated>cargo build --features python 2>&1 | tail -5 && cargo clippy --all-targets --features python -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'mod py_gamma;' src/pymodels/mod.rs` and `grep -q 'pub use py_gamma::PyGamma;' src/pymodels/mod.rs`.
    - `grep -q 'PyGamma' src/lib.rs`.
    - `grep -c '"Gamma"' python/polars_statistics/__init__.py` is >= 1 and `grep -q 'Gamma,' python/polars_statistics/__init__.py`.
    - `cargo build --features python` exits 0.
  </acceptance_criteria>
  <done>Gamma is declared and exported in mod.rs, added to the pymodule in lib.rs, and imported + listed in __all__ in __init__.py; the workspace builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → PyO3 (`fit`/`predict`) | User-provided numpy f64 arrays cross into Rust; shape/positivity not guaranteed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | `PyGamma::fit` on singular/degenerate design | low | mitigate | `GammaRegressor::fit` returns `Err` on rank/convergence failure; mapped to `PyValueError` (no panic) |
| T-04-02 | Tampering | Non-positive `y` passed to Gamma IRLS | low | accept | Crate IRLS surfaces a fit error mapped to `PyValueError`; documented in the smoke test using positive `y` |
</threat_model>

<verification>
- `cargo build --features python` and `cargo clippy --all-targets --features python -- -D warnings` and `cargo fmt --check` all green.
- `Gamma` importable and registered in all three files (grep checks above).
- pytest smoke (`tests/test_gamma.py`) deferred to the Plan 06 phase gate (maturin+pytest run by orchestrator).
</verification>

<success_criteria>
- REGR-03 satisfied: `Gamma` PyModel exists, is registered end-to-end, and is callable from Python.
- Tracer pattern established for all subsequent Phase 4 PyModel additions.
</success_criteria>

<output>
Create `.planning/phases/04-regression-api-parity/04-01-SUMMARY.md` when done.
</output>
