---
phase: 06-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_statistics_parity.py
autonomous: true
requirements: [TEST-01, TEST-03]
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "one_way_anova F/p output is validated against scipy.stats.f_oneway (per D: TEST-03 scipy strategy)"
    - "A reusable reference-lib skip guard exists so the runtime wheel stays dependency-light"
  artifacts:
    - tests/conftest.py
    - tests/test_statistics_parity.py
  key_links:
    - "test_statistics_parity.py imports the scipy/statsmodels importorskip helper from conftest"
---

<objective>
Tracer: establish the value-validation-against-reference pattern for the whole phase by wiring
ONE representative statistics test end-to-end — `one_way_anova` validated against
`scipy.stats.f_oneway` — plus a reusable pytest fixture/helper that skips reference-library tests
when scipy/statsmodels are absent (keeping the runtime wheel dependency-light per CONTEXT decision).

Purpose: Prove the scipy-reference validation pattern on the agent's best early-context tokens so
plans 06-02/03 can replicate it mechanically. scipy and statsmodels are already declared in the
pyproject `dev` extras — this plan only adds the import guard and the first value assertion.
Output: A `require_scipy` (and `require_statsmodels`) skip helper in conftest.py, and an upgraded
one_way_anova test asserting F-statistic and p-value equality vs scipy within tolerance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-validation/06-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reference-lib skip guard + scipy-validated one_way_anova (end-to-end pattern)</name>
  <files>tests/conftest.py, tests/test_statistics_parity.py</files>
  <read_first>
    - tests/conftest.py (existing fixtures — add helpers alongside, do not remove)
    - tests/test_statistics_parity.py (existing one_way_anova smoke test to upgrade — do not duplicate)
    - python/polars_statistics/exprs/parametric.py (def one_way_anova — exact call signature and output struct field names)
    - .planning/phases/06-testing-validation/06-CONTEXT.md (TEST-03 scipy/statsmodels validation decision)
  </read_first>
  <action>
    In conftest.py add two module-level skip helpers using pytest.importorskip: one that imports
    and returns scipy (name it require_scipy as a pytest fixture) and one for statsmodels
    (require_statsmodels). Guard each import so a missing library skips the test rather than
    erroring, keeping the runtime wheel dependency-light per the CONTEXT decision. Then upgrade the
    existing one_way_anova case in test_statistics_parity.py (do NOT add a second duplicate test):
    build a small multi-group dataset via the one_way_anova Polars expression, extract the F and
    p-value output struct fields, and assert they match scipy.stats.f_oneway on the same groups
    within a loose tolerance appropriate to the statistic. Use the require_scipy fixture so the
    assertion is skipped when scipy is unavailable. Reference the field names exactly as defined in
    parametric.py's output struct.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/conftest.py').read()); ast.parse(open('tests/test_statistics_parity.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py defines require_scipy and require_statsmodels using pytest.importorskip
    - test_statistics_parity.py references scipy.stats.f_oneway and asserts F/p equality vs it
    - grep -v '^#' tests/test_statistics_parity.py | grep -c 'f_oneway' returns >= 1
    - No new library source symbols were added (tests-only change)
  </acceptance_criteria>
  <done>The one_way_anova expression output is asserted equal to scipy.stats.f_oneway within
  tolerance, guarded by an importorskip fixture, establishing the reference-validation pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-dep install | scipy/statsmodels pulled into the dev/test environment only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | scipy/statsmodels (test-only deps, already in pyproject dev extras) | low | accept | Well-known, widely-audited scientific libraries; test-only (importorskip-guarded), never shipped in the runtime wheel |
</threat_model>

<verification>
- conftest.py and test_statistics_parity.py parse as valid Python.
- The one_way_anova value assertion references scipy.stats.f_oneway.
- Full maturin+pytest run is deferred to the phase-gate plan (06-05), run by the orchestrator.
</verification>

<success_criteria>
one_way_anova is value-validated against scipy, and a reusable reference-lib skip guard exists for
all downstream value tests.
</success_criteria>

<output>
Create `.planning/phases/06-testing-validation/06-01-SUMMARY.md` when done.
</output>
