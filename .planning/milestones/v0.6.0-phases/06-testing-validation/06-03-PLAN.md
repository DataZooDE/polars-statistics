---
phase: 06-testing-validation
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tests/test_gamma.py
  - tests/test_glm_diagnostics.py
  - tests/test_glmm.py
  - tests/test_pspline.py
  - tests/test_robust_regression.py
  - tests/test_theil_sen.py
  - tests/test_ransac.py
  - tests/test_bayesian.py
  - tests/test_lars.py
  - tests/test_pa.py
  - tests/test_moments.py
  - tests/test_mc_dispersion.py
autonomous: true
requirements: [TEST-02, TEST-03]
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Gamma GLM coefficients/dispersion are value-validated against statsmodels Gamma GLM"
    - "TheilSen/RANSAC/BayesianRidge/ARD/LARS coefficients are validated against sklearn or analytic ground truth"
    - "GLMM, PSpline, PassiveAggressive, MomentAccumulator, and the 5 gamma_* diagnostics have value/analytic assertions"
    - "Ridge hc_inference returns HC SEs; WLS hc_inference asserts NotImplementedError (per Phase-4 decision)"
    - "OLS/Ridge fit_from_accumulator matches the standard fit within tolerance"
  artifacts:
    - tests/test_gamma.py
    - tests/test_glm_diagnostics.py
    - tests/test_moments.py
  key_links:
    - "regression value tests reuse require_statsmodels/require_scipy from conftest.py (06-01)"
---

<objective>
Upgrade the existing shape-only smoke tests for the newly exposed REGRESSION capabilities from
shape-only to value-correctness, validating against statsmodels/sklearn or analytic ground truth
where feasible, following the reference pattern from the 06-01 tracer.

Purpose: Satisfy TEST-02 (shape+value per new regression capability) and TEST-03 (reference
validation where available). Covers Gamma, GLMM, PSpline, TheilSen, RANSAC, BayesianRidge, ARD,
LARS, PassiveAggressive, MomentAccumulator, Ridge/WLS hc_inference, OLS/Ridge fit_from_accumulator,
and the 5 gamma_* diagnostics.
Output: Value assertions added to the existing regression test files — no duplicates, no new
library symbols. sklearn is NOT a declared dependency, so validate against sklearn only when
present via an importorskip guard; otherwise use analytic ground truth.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-validation/06-CONTEXT.md
@.planning/phases/02-public-api-audit/02-API-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Value-validate Gamma GLM + the 5 gamma_* diagnostics</name>
  <files>tests/test_gamma.py, tests/test_glm_diagnostics.py, tests/test_mc_dispersion.py</files>
  <read_first>
    - tests/test_gamma.py (existing Gamma smoke test — upgrade in place)
    - tests/test_glm_diagnostics.py (existing importorskip usage + gamma diagnostic smoke cases)
    - tests/test_mc_dispersion.py (existing dispersion smoke cases)
    - tests/conftest.py (require_statsmodels helper from 06-01)
    - python/polars_statistics/exprs/regression.py (def gamma_dispersion_deviance, gamma_dispersion_pearson, gamma_pearson_chi_squared, gamma_standardized_deviance_residuals, gamma_standardized_pearson_residuals — signatures + fields)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (REGR-03/REGR-05 gap definitions)
  </read_first>
  <action>
    Upgrade the Gamma PyModel test to fit a Gamma GLM on a dataset and assert its coefficients (and
    dispersion where exposed) match statsmodels GLM with a Gamma family + log link within a loose
    tolerance, guarded by require_statsmodels. For the 5 gamma_* diagnostic expressions
    (gamma_dispersion_deviance, gamma_dispersion_pearson, gamma_pearson_chi_squared,
    gamma_standardized_deviance_residuals, gamma_standardized_pearson_residuals), add value/analytic
    assertions: dispersion estimates must be positive and finite and consistent between the deviance
    and Pearson forms on a well-conditioned dataset; pearson_chi_squared must equal the sum of
    squared Pearson residuals (analytic identity); standardized residuals must have near-unit spread.
    Extend the existing cases rather than adding duplicate tests.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/test_gamma.py','tests/test_glm_diagnostics.py','tests/test_mc_dispersion.py']]"</automated>
  </verify>
  <acceptance_criteria>
    - test_gamma.py asserts Gamma coefficients vs statsmodels (require_statsmodels-guarded)
    - All 5 gamma_* diagnostics have a value or analytic-identity assertion
    - grep -v '^#' tests/test_glm_diagnostics.py | grep -c 'gamma_pearson_chi_squared' returns >= 1
  </acceptance_criteria>
  <done>Gamma GLM and its 5 diagnostics are value-validated (statsmodels or analytic identity).</done>
</task>

<task type="auto">
  <name>Task 2: Value-validate robust/Bayesian/path regressors (TheilSen, RANSAC, BayesianRidge, ARD, LARS)</name>
  <files>tests/test_theil_sen.py, tests/test_ransac.py, tests/test_bayesian.py, tests/test_lars.py, tests/test_robust_regression.py</files>
  <read_first>
    - tests/test_theil_sen.py, tests/test_ransac.py, tests/test_bayesian.py, tests/test_lars.py, tests/test_robust_regression.py (existing smoke cases to upgrade)
    - tests/conftest.py (skip helpers)
    - python/polars_statistics/__init__.py (TheilSen, RANSAC, BayesianRidge, ARD, LARS class signatures — fit/coef getters)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (REGR-06 gap definitions; sklearn-parity notes)
  </read_first>
  <action>
    Upgrade each regressor's smoke test to a value test on a dataset with known ground-truth
    coefficients (y = a + b*x + noise). Assert recovered slope/intercept are close to the true
    coefficients within a loose tolerance. Where sklearn is available (importorskip-guard,
    sklearn is NOT a declared dep so it must be optional), additionally assert coefficients match
    the corresponding sklearn estimator (TheilSenRegressor, RANSACRegressor, BayesianRidge,
    ARDRegression, Lars/LassoLars) within tolerance. For TheilSen/RANSAC, plant outliers and assert
    the robust fit stays near the true line where OLS would be pulled away. Extend existing cases;
    do not duplicate.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/test_theil_sen.py','tests/test_ransac.py','tests/test_bayesian.py','tests/test_lars.py','tests/test_robust_regression.py']]"</automated>
  </verify>
  <acceptance_criteria>
    - Each of TheilSen/RANSAC/BayesianRidge/ARD/LARS asserts recovered coefficients vs known ground truth
    - sklearn comparisons are importorskip-guarded (no hard sklearn dependency)
    - TheilSen and RANSAC tests include a planted-outlier robustness assertion
  </acceptance_criteria>
  <done>All five robust/Bayesian/path regressors are value-validated against ground truth (and
  sklearn when present).</done>
</task>

<task type="auto">
  <name>Task 3: Value-validate GLMM, PSpline, PassiveAggressive, MomentAccumulator, HC + accumulator paths</name>
  <files>tests/test_glmm.py, tests/test_pspline.py, tests/test_pa.py, tests/test_moments.py</files>
  <read_first>
    - tests/test_glmm.py, tests/test_pspline.py, tests/test_pa.py, tests/test_moments.py (existing smoke cases)
    - tests/conftest.py (skip helpers)
    - python/polars_statistics/__init__.py (GLMM, PSpline, PassiveAggressive, MomentAccumulator class signatures; OLS/Ridge fit_from_accumulator; Ridge/WLS hc_inference)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (REGR-01/02/06 notes; WLS hc NotImplementedError decision)
  </read_first>
  <action>
    Upgrade each smoke test to a value test. GLMM: fit a Gaussian random-intercept model on data
    with a known fixed-effect slope and assert the fixed-effect estimate is close to truth and the
    factor variance is positive; assert the FactorSummary output structure is populated. PSpline:
    fit on data sampled from a smooth function and assert fitted values track the underlying curve
    (low residual RMSE) — a smoothing property, not exact coefficients. PassiveAggressive: assert
    that after partial_fit/fit on a separable-ish linear target the predictions approach the target
    within tolerance. MomentAccumulator: build the accumulator over a dataset, then assert
    OLS/Ridge fit_from_accumulator coefficients equal the standard OLS/Ridge fit on the same data
    within tight tolerance (analytic identity). Also assert Ridge.hc_inference returns finite HC
    standard errors, and WLS.hc_inference raises NotImplementedError (keep the Phase-4 decision).
    Extend existing cases; do not duplicate.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/test_glmm.py','tests/test_pspline.py','tests/test_pa.py','tests/test_moments.py']]"</automated>
  </verify>
  <acceptance_criteria>
    - GLMM asserts fixed-effect estimate near truth + positive factor variance + populated FactorSummary
    - PSpline asserts fitted values track the underlying smooth curve (residual RMSE bound)
    - MomentAccumulator: OLS/Ridge fit_from_accumulator coefficients equal standard fit within tolerance
    - Ridge.hc_inference returns finite HC SEs; WLS.hc_inference asserts NotImplementedError
  </acceptance_criteria>
  <done>GLMM, PSpline, PassiveAggressive, MomentAccumulator, HC (Ridge/WLS), and the
  fit_from_accumulator path all carry value or analytic assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-dep import | statsmodels/scipy/sklearn used only inside importorskip-guarded value tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | statsmodels/scipy/sklearn (test-only; scipy+statsmodels already in dev extras; sklearn optional-guarded) | low | accept | Widely-audited scientific libs, test-only, never in the runtime wheel; sklearn is importorskip-guarded so it is not a required dependency |
</threat_model>

<verification>
- All edited regression test files parse as valid Python.
- Each new regression capability carries a value or analytic assertion.
- Full maturin+pytest run deferred to 06-05 (orchestrator-run phase gate).
</verification>

<success_criteria>
Every newly exposed regression capability has a value-correctness assertion (reference-lib or
analytic), including HC and fit_from_accumulator paths.
</success_criteria>

<output>
Create `.planning/phases/06-testing-validation/06-03-SUMMARY.md` when done.
</output>
