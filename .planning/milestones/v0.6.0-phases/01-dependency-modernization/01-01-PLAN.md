---
phase: 01-dependency-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "Cargo.toml pins anofox-statistics = \"0.4.2\" and anofox-regression = \"0.5.13\" (DEP-01, DEP-02)"
    - "Cargo.lock resolves both crates to the bumped versions with no unrelated dep changes"
    - "cargo build --features python succeeds against the upgraded crates (DEP-01, DEP-02)"
    - "cargo clippy --all-targets -- -D warnings passes clean; existing behavior preserved (DEP-03)"
  artifacts:
    - "Cargo.toml (two version pins edited)"
    - "Cargo.lock (regenerated, scoped to the two anofox crates)"
  key_links:
    - "Cargo.toml version strings -> cargo update -> Cargo.lock -> cargo build --features python compile"
---

<objective>
Bump both backing engine crates to their target versions and prove the wrapper still
compiles end-to-end under the `python` feature and clippy `-D warnings`.

This is the TRACER slice for Phase 1: the thinnest path that touches every layer the
phase modifies (Cargo.toml version strings -> Cargo.lock -> full wrapper compile), wired
end-to-end and verified before any expansion. If the crate bump breaks compilation, this
task surfaces it in the first commit rather than after tests are written.

Purpose: Establish the modernized dependency foundation every later phase builds on, and
inherit the 0.5.13 column-pivot correctness fix (confirmed active by Plan 02).
Output: Cargo.toml with two bumped pins, regenerated Cargo.lock, a clean compile + lint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-modernization/01-RESEARCH.md
@.planning/phases/01-dependency-modernization/01-PATTERNS.md
@Cargo.toml
</context>

<artifacts_this_phase_produces>
This plan (Plan 01) produces NO new code symbols. It edits two existing version pins in
`Cargo.toml` (lines 64 and 67) and regenerates `Cargo.lock`. The new `#[test]` functions
are produced by Plan 02, not here. No new public API, no new modules, no new functions.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Bump both anofox crate pins and prove the wrapper compiles end-to-end</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - Cargo.toml (lines 63-67 — the two existing pins `anofox-regression = "0.5.4"` on line 64 and `anofox-statistics = "0.4.1"` on line 67)
    - .planning/phases/01-dependency-modernization/01-RESEARCH.md (Standard Stack + Wrapper Call-Site Inventory + Common Pitfalls sections)
    - .planning/phases/01-dependency-modernization/01-PATTERNS.md ("Cargo.toml — version pin edit" section)
  </read_first>
  <action>
    Edit exactly two lines in Cargo.toml, changing only the version strings and nothing else:
    change line 64 `anofox-regression = "0.5.4"` to `anofox-regression = "0.5.13"` (DEP-02),
    and change line 67 `anofox-statistics = "0.4.1"` to `anofox-statistics = "0.4.2"` (DEP-01).

    Then regenerate the lock file with a SCOPED update — run `cargo update -p anofox-statistics -p anofox-regression`.
    Do NOT run a bare `cargo update`: that updates all deps within semver (polars 0.52, pyo3 0.26)
    and can introduce unrelated breakage (per RESEARCH Pitfall 1). Both target versions are already
    in the local registry cache, so no network fetch is required.

    Confirm the resolve is scoped: inspect the Cargo.lock diff and verify only the two anofox
    crates changed version (3-4 lines total is the expected ceiling); if more packages changed,
    revert Cargo.lock and re-run the scoped `cargo update` with the `-p` flags.

    Now prove the full wrapper compiles against the upgraded crates. Per RESEARCH, zero wrapper
    reconciliation is expected — the call-site inventory confirms every imported symbol from both
    crates is present and unchanged in the bumped versions, and the new `converged: bool` fields on
    the Fitted* GLM structs are binary-compatible because the wrapper never constructs those structs
    by struct-literal (it only calls `regressor.fit(...)`). So DEP-03 reconciliation is expected to
    be a no-op. IF `cargo build --features python` or clippy DOES surface a breakage, reconcile it
    MINIMALLY — fix only the broken call signature / import path to preserve existing behavior; do
    NOT refactor working surface and do NOT add or expose any new API (that is Phases 3-4, out of
    scope here). Record any such reconciliation in the plan SUMMARY.

    Run `cargo build --features python` (proves DEP-01/DEP-02 build gate through the full PyO3 code
    path, not just `--no-default-features`), then `cargo clippy --all-targets -- -D warnings` (matches
    CI, covers test targets — DEP-03).
  </action>
  <verify>
    <automated>grep -q 'anofox-regression = "0.5.13"' Cargo.toml && grep -q 'anofox-statistics = "0.4.2"' Cargo.toml && grep -q 'name = "anofox-regression"' Cargo.lock && grep -A1 'name = "anofox-regression"' Cargo.lock | grep -q 'version = "0.5.13"' && grep -A1 'name = "anofox-statistics"' Cargo.lock | grep -q 'version = "0.4.2"' && cargo build --features python && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'anofox-regression = "0.5.13"' Cargo.toml` matches (exit 0)
    - `grep 'anofox-statistics = "0.4.2"' Cargo.toml` matches (exit 0)
    - Cargo.lock contains `version = "0.5.13"` under `name = "anofox-regression"` and `version = "0.4.2"` under `name = "anofox-statistics"`
    - The Cargo.lock diff changed only the two anofox crate entries (no polars/pyo3/other version churn)
    - `cargo build --features python` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0 (no warnings)
  </acceptance_criteria>
  <reversibility rating="reversible">Version pins and Cargo.lock are trivially revertible via git; no data or external state is mutated.</reversibility>
  <done>
    Cargo.toml pins read 0.4.2 (statistics) and 0.5.13 (regression), Cargo.lock resolves both
    to those versions with no unrelated dep changes, `cargo build --features python` succeeds,
    and `cargo clippy --all-targets -- -D warnings` passes clean. DEP-01, DEP-02, DEP-03 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io registry -> local build | Dependency source crates are pulled into the build; already resolved from the local registry cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | cargo update of anofox-statistics/anofox-regression | low | accept | Both crates are the project's existing first-party dependencies (bump only, no new packages); both target versions resolve from the already-cached local registry with no new transitive network dependency added by intent. Package Legitimacy Audit in RESEARCH.md approves both. Scoped `-p` update prevents unrelated dep drift. No new runtime attack surface or user-facing input path is introduced by a trusted version bump. |

No `high`-severity threats: this phase bumps trusted first-party crate versions and adds no new runtime input path, network call, auth, or cryptography surface.
</threat_model>

<verification>
- Cargo.toml pins: `grep` for both target version strings (exact match)
- Cargo.lock resolution: `grep` for `version = "0.5.13"` / `version = "0.4.2"` under the crate names
- Scoped resolve: `git diff Cargo.lock` shows only the two anofox crate entries changed
- Build gate: `cargo build --features python` exits 0
- Lint gate: `cargo clippy --all-targets -- -D warnings` exits 0
</verification>

<success_criteria>
- anofox-statistics reads 0.4.2 and anofox-regression reads 0.5.13 in both Cargo.toml and Cargo.lock
- Workspace builds with the `python` feature
- Wrapper compiles clean under clippy `-D warnings` with existing behavior preserved
</success_criteria>

<output>
Create `.planning/phases/01-dependency-modernization/01-01-SUMMARY.md` when done.
Record whether any DEP-03 reconciliation was needed (expected: none), and paste the
`git diff Cargo.lock` package-count confirmation.
</output>
