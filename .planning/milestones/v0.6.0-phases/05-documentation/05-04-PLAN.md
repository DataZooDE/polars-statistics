---
phase: 05-documentation
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [DOCS-04]
estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "CHANGELOG.md has a 0.6.0 section grouping the new statistics and regression API"
    - "The 0.6.0 section records the anofox crate bumps (statistics 0.4.2, regression 0.5.13) and the inherited column-pivot correctness fix"
    - "The 0.6.0 section documents the two known limitations: WLS hc_inference NotImplementedError and the ICC contract change"
  artifacts:
    - CHANGELOG.md
  key_links:
    - "0.6.0 section sits above 0.5.0 and follows the Keep-a-Changelog Added/Changed format already used in the file"
---

<objective>
DOCS-04: complete the CHANGELOG 0.6.0 section (seeded by the 05-01 tracer) so it documents the full
0.6.0 release — the new statistics + regression API grouped, the anofox crate bumps, the inherited
column-pivot correctness fix, and the known limitations.

Purpose: Users and release notes have an accurate, grouped record of everything shipped in 0.6.0
(DOCS-04). Note: the actual version bump in Cargo.toml/pyproject.toml and the PyPI publish are
deferred to Phase 7 (per 05-CONTEXT.md Deferred Ideas) — this plan writes the CHANGELOG text only.
Output: A complete, Keep-a-Changelog-formatted 0.6.0 section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/02-public-api-audit/02-API-AUDIT.md
@.planning/phases/03-statistics-api-parity/03-04-SUMMARY.md
@.planning/phases/04-regression-api-parity/04-06-SUMMARY.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the CHANGELOG 0.6.0 section (DOCS-04)</name>
  <files>CHANGELOG.md, .planning/phases/05-documentation/check_changelog.py</files>
  <read_first>
    - CHANGELOG.md (the 0.6.0 section stub seeded by 05-01 with one_way_anova; the 0.5.0 section as the format template — Added/Changed/Backwards-compatibility groups + the compare-link footer block at the bottom)
    - .planning/phases/03-statistics-api-parity/03-04-SUMMARY.md (statistics delivered: one_way_anova, two_way_anova, repeated_measures_anova, energy_distance_nd, real matrix-input icc)
    - .planning/phases/04-regression-api-parity/04-06-SUMMARY.md (regression delivered: Gamma, GLMM, PSpline, TheilSen, RANSAC, BayesianRidge, ARD, LARS, PassiveAggressive, MomentAccumulator; Ridge/WLS hc_inference; OLS/Ridge fit_from_accumulator; 5 gamma_* diagnostics)
    - .planning/STATE.md (decisions: bump BOTH crates; regression 0.5.13 carries the column-pivot correctness fix)
  </read_first>
  <action>
    Expand the 0.6.0 section (keep it above 0.5.0, preserve the tracer's one_way_anova entry) into a
    complete Keep-a-Changelog entry. `### Added`: group the new API — "New statistics API" (one_way_anova,
    two_way_anova, repeated_measures_anova, energy_distance_nd, matrix-input icc) and "New regression
    API" (Gamma, GLMM, PSpline, TheilSen, RANSAC, BayesianRidge, ARD, LARS, PassiveAggressive,
    MomentAccumulator PyModels; Ridge/WLS hc_inference; OLS/Ridge fit_from_accumulator; the 5 gamma_*
    GLM diagnostic expressions). `### Changed`: record the dependency bumps anofox-statistics 0.4.1 ->
    0.4.2 and anofox-regression 0.5.4 -> 0.5.13, and the inherited column-pivot correctness fix (OLS/
    WLS/NNLS on differently-scaled designs now return correct coefficients). Add a "### Known
    limitations" (or Notes) group documenting: WLS `hc_inference` raises NotImplementedError, and the
    ICC contract change (new matrix-input signature replacing the former all-NaN stub) — both per the
    05-CONTEXT.md specifics. Add a `[0.6.0]` compare-link line to the footer block consistent with the
    existing footer style. Do NOT bump versions in Cargo.toml/pyproject.toml (Phase 7). Finally, author
    check_changelog.py — a checker that reads CHANGELOG.md and exits non-zero unless it finds the 0.6.0
    heading, every new statistics + regression symbol name, both crate versions (0.4.2 and 0.5.13), and
    both limitation keywords (NotImplementedError and ICC).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && python .planning/phases/05-documentation/check_changelog.py</automated>
  </verify>
  <done>The 0.6.0 section lists all new statistics + regression API, both crate bumps, the column-pivot fix, and the two limitations; the checker exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only: CHANGELOG prose; no runtime code path or untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Repudiation | release changelog accuracy | low | mitigate | The checker asserts every shipped symbol and both crate bumps are recorded, so the release record cannot silently omit shipped API. No package installs, so no legitimacy gate needed. |
</threat_model>

<verification>
- check_changelog.py exits 0 (asserts 0.6.0 heading; presence of the new statistics + regression symbol
  names; both crate versions 0.4.2 and 0.5.13; and the two limitation keywords NotImplementedError + ICC)
</verification>

<success_criteria>
CHANGELOG.md contains a complete, correctly formatted 0.6.0 section covering the full release —
DOCS-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-documentation/05-04-SUMMARY.md` when done.
</output>
