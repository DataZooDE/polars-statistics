---
phase: 05-documentation
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - mkdocs.yml
  - docs/api/tests/parametric.md
  - docs/api/tests/correlation.md
  - docs/api/regression/glm.md
  - docs/api/regression/linear.md
  - docs/api/regression/diagnostics.md
  - docs/api/classes/glm.md
  - docs/api/classes/linear.md
  - docs/API_REFERENCE.md
  - docs/api/README.md
autonomous: true
requirements: [DOCS-02]
estimate:
  tokens: 65000
  raw_tokens: 32500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The 4 new statistics expressions (two_way_anova, repeated_measures_anova, energy_distance_nd, icc) appear on their mkdocs API reference pages"
    - "The 10 new regression PyModels and 5 gamma_* diagnostic expressions appear on the appropriate mkdocs regression/classes pages"
    - "The mkdocs nav lists every reference page and the docs build (or fallback reference check) is clean"
  artifacts:
    - docs/api/regression/glm.md
    - docs/api/classes/glm.md
  key_links:
    - "mkdocs build --strict resolves every nav reference and internal link with no errors"
---

<objective>
DOCS-02: surface the entire remaining newly exposed API on the mkdocs reference site — the 4 new
statistics expressions (all except one_way_anova, done in the 05-01 tracer), the 10 new regression
PyModel classes, the Ridge/WLS HC additions, OLS/Ridge fit_from_accumulator + MomentAccumulator, and
the 5 gamma_* diagnostic expressions — and keep the docs build clean.

Purpose: Every new symbol is discoverable in the published API reference (DOCS-02), following the
existing docs/ layout (Claude's-Discretion decision in 05-CONTEXT.md: do not restructure the site).
Output: New per-symbol sections added to the existing reference pages, nav updated only where a new
page is genuinely needed, and a clean `mkdocs build` (or the fallback nav-reference check).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/02-public-api-audit/02-API-AUDIT.md
@.planning/phases/05-documentation/05-01-SUMMARY.md
@mkdocs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reference sections for the new statistics expressions and regression diagnostics (DOCS-02)</name>
  <files>docs/api/tests/parametric.md, docs/api/tests/correlation.md, docs/api/regression/diagnostics.md</files>
  <read_first>
    - docs/api/tests/parametric.md (the one_way_anova section added by the 05-01 tracer — copy its exact section shape for two_way_anova and repeated_measures_anova)
    - docs/api/tests/correlation.md (existing icc content + existing per-symbol format; confirm where energy_distance / modern-test content lives)
    - docs/api/regression/diagnostics.md (existing diagnostic-expression section format — add the 5 gamma_* diagnostics here)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (target-surface column per symbol)
  </read_first>
  <action>
    Add per-symbol reference sections (matching each page's existing `## \`name\`` format: description,
    python signature fence, Returns struct, Example) for: two_way_anova and repeated_measures_anova on
    docs/api/tests/parametric.md; energy_distance_nd where the existing energy_distance / modern-test
    content lives (read to confirm the page — do not create a new page if one already covers it); the
    matrix-input icc on docs/api/tests/correlation.md (update the existing icc content to the new
    contract, noting it replaced the all-NaN stub); and the 5 gamma_* diagnostic expressions
    (gamma_dispersion_deviance, gamma_dispersion_pearson, gamma_pearson_chi_squared,
    gamma_standardized_pearson_residuals, gamma_standardized_deviance_residuals) on
    docs/api/regression/diagnostics.md. Do not restructure pages. Satisfies DOCS-02 for the expression
    surface.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && grep -q 'two_way_anova' docs/api/tests/parametric.md && grep -q 'repeated_measures_anova' docs/api/tests/parametric.md && grep -rq 'energy_distance_nd' docs/api/tests/ && grep -q 'gamma_dispersion_deviance' docs/api/regression/diagnostics.md && grep -q 'gamma_standardized_deviance_residuals' docs/api/regression/diagnostics.md && echo EXPR_DOCS_OK</automated>
  </verify>
  <done>All 4 new statistics expressions and all 5 gamma_* diagnostics have reference sections on their existing pages.</done>
</task>

<task type="auto">
  <name>Task 2: Add reference sections for the 10 new PyModels + HC/accumulator, update nav, prove clean build (DOCS-02)</name>
  <files>docs/api/regression/linear.md, docs/api/regression/glm.md, docs/api/classes/linear.md, docs/api/classes/glm.md, docs/API_REFERENCE.md, docs/api/README.md, mkdocs.yml, .planning/phases/05-documentation/check_docs_symbols.py</files>
  <read_first>
    - docs/api/classes/linear.md and docs/api/classes/glm.md (existing PyModel-class section format — the "Model Classes" nav group)
    - docs/api/regression/linear.md and docs/api/regression/glm.md (existing regression-model page format)
    - docs/API_REFERENCE.md and docs/api/README.md (the API index/overview lists — add the new names)
    - mkdocs.yml (nav — Model Classes > Linear/GLM and Regression groups; only add a nav entry if a genuinely new page is created)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (REGR-01..06 class list + target surfaces)
  </read_first>
  <action>
    Document the 10 new PyModel classes on the appropriate existing "Model Classes" / "Regression"
    pages, grouped by type: robust/linear (TheilSen, RANSAC, BayesianRidge, ARD, LARS,
    PassiveAggressive) on the linear pages; GLM (Gamma, GLMM, PSpline) on the glm pages;
    MomentAccumulator plus OLS/Ridge fit_from_accumulator on the linear pages; note Ridge/WLS
    hc_inference (WLS raises NotImplementedError) on the relevant class page. Each section follows the
    existing class-page format (class name, constructor signature, methods, one runnable example). Add
    the new names to the API_REFERENCE.md / api/README.md index lists. Update mkdocs.yml nav ONLY if you
    create a genuinely new page (prefer adding sections to existing pages per the do-not-restructure
    decision). Then author check_docs_symbols.py — a checker that greps docs/ recursively and exits
    non-zero unless every new symbol appears at least once: the 4 statistics expressions (two_way_anova,
    repeated_measures_anova, energy_distance_nd, icc), the 10 PyModels (Gamma, GLMM, PSpline, TheilSen,
    RANSAC, BayesianRidge, ARD, LARS, PassiveAggressive, MomentAccumulator), and the 5 gamma_*
    diagnostics — and run it plus the shared docs-build checker to prove no reference/link broke.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && python .planning/phases/05-documentation/check_docs_symbols.py && python .planning/phases/05-documentation/check_docs_build.py && echo CLASS_DOCS_OK</automated>
  </verify>
  <done>All 10 new PyModels + Gamma appear somewhere under docs/api/; check_docs_build.py exits 0 (clean build or clean nav fallback).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only: markdown reference pages and nav; no runtime code path or untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | mkdocs nav / internal links | low | mitigate | `mkdocs build --strict` (or nav-reference fallback) fails the build on any broken link, catching mis-wired pages before release. No package installs, so no legitimacy gate needed. |
</threat_model>

<verification>
- check_docs_symbols.py exits 0 (asserts every new statistics + regression symbol appears under docs/)
- check_docs_build.py exits 0 (clean mkdocs build or clean nav-reference fallback)
- The 5 gamma_* diagnostics and 4 new statistics expressions appear on their reference pages
</verification>

<success_criteria>
Every newly exposed symbol is discoverable on the mkdocs API reference and the docs build is clean —
DOCS-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-documentation/05-03-SUMMARY.md` when done (note whether mkdocs ran a real build or the fallback was used).
</output>
