---
phase: 05-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/polars_statistics/exprs/parametric.py
  - src/expressions/parametric.rs
  - docs/api/tests/parametric.md
  - CHANGELOG.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "one_way_anova has a Python docstring with signature, parameters, and a runnable example"
    - "one_way_anova appears on an mkdocs API reference page and the docs build cleanly"
    - "The Rust *_fit wrapper for one_way_anova carries a /// doc comment"
    - "A CHANGELOG 0.6.0 section exists and lists one_way_anova"
  artifacts:
    - docs/api/tests/parametric.md
    - CHANGELOG.md
  key_links:
    - "docs build (mkdocs) sees the new parametric.md entry with no broken references"
---

<objective>
Tracer doc slice: fully document ONE representative new symbol — `one_way_anova` — across all four
documentation surfaces (Python docstring, mkdocs reference page, Rust `///` doc comment, CHANGELOG
0.6.0 entry), and prove the docs build pipeline is clean end-to-end. This validates the doc-tooling
path (especially `mkdocs build`) before the expansion plans document the remaining ~25 symbols.

Purpose: De-risk the docs pipeline (mkdocs availability, page wiring, CHANGELOG format) on one symbol
per the Claude's-Discretion decisions in 05-CONTEXT.md (D-01..D-04: follow existing conventions).
Output: `one_way_anova` documented on all four surfaces; a 0.6.0 CHANGELOG section seeded; the docs
site builds (or the fallback presence check passes) so expansion plans 05-02/03/04 extend it safely.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/02-public-api-audit/02-API-AUDIT.md
@mkdocs.yml
</context>

<artifacts>
## Artifacts this phase produces (documentation only — no code behavior changes)
- Python docstrings in `python/polars_statistics/exprs/*.py` (audit + fill)
- Rust `///` item docs in `src/expressions/*.rs` and `src/pymodels/*.rs`. NOTE: the new PyModel
  classes (Gamma, GLMM, PSpline, TheilSen, RANSAC, BayesianRidge, ARD, LARS, PassiveAggressive,
  MomentAccumulator) are imported from the Rust extension, so their PyO3 `///` class/method doc
  comments ARE the Python class docstrings — they satisfy BOTH DOCS-01 (class docstring) and DOCS-03
  (Rust doc comment) at once.
- mkdocs reference pages under `docs/api/tests/`, `docs/api/regression/`, `docs/api/classes/`, and
  `docs/API_REFERENCE.md` / `docs/api/README.md`
- A `0.6.0` section in `CHANGELOG.md`

This tracer plan produces only the `one_way_anova` slice of the above.
</artifacts>

<tasks>

<task type="tracer">
  <name>Task 1: Document one_way_anova end-to-end across all four surfaces (DOCS-01/02/03/04)</name>
  <files>python/polars_statistics/exprs/parametric.py, src/expressions/parametric.rs, docs/api/tests/parametric.md, CHANGELOG.md</files>
  <read_first>
    - python/polars_statistics/exprs/parametric.py (the `one_way_anova` builder near line 14; confirm the existing numpy-style docstring already has a runnable Examples block with `>>>` lines, matching the ttest_ind pattern)
    - src/expressions/parametric.rs (find the public `one_way_anova`-related `*_fit` wrapper fn; note whether a `///` doc comment is already present)
    - docs/api/tests/parametric.md (existing per-symbol section format: `## \`name\``, description, python signature fence, `**Returns:**` struct line, Example — mirror it exactly)
    - CHANGELOG.md (top-of-file section format; latest is `## [0.5.0]`; Keep-a-Changelog `### Added` / `### Changed` groups)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (STAT-01 row confirms one_way_anova is a new symbol)
  </read_first>
  <action>
    Audit-and-fill for the single symbol one_way_anova. (1) DOCS-01: in parametric.py, ensure the
    one_way_anova docstring documents the signature, every parameter (including the `kind` selector),
    and a RUNNABLE Examples block using a small inline pl.DataFrame consistent with the smoke tests —
    fill any gap, do not rewrite a complete docstring. (2) DOCS-03: in src/expressions/parametric.rs,
    add or complete a `///` doc comment on the public one_way_anova `*_fit` wrapper fn describing what
    it computes and its output struct, matching the existing `///` style in that file. (3) DOCS-02:
    add a section for one_way_anova to docs/api/tests/parametric.md following the exact section format
    used by ttest_ind (description, python signature fence, Returns struct, Example); place it with the
    other parametric tests and do not restructure the page or nav. (4) DOCS-04: add a new 0.6.0 section
    at the very top of CHANGELOG.md (above the 0.5.0 section) with an `### Added` group listing
    one_way_anova under a "New statistics API" bullet — this seeds the section that plan 05-04 fills
    out; keep the Keep-a-Changelog format. Per D-01..D-04 these are Claude's-Discretion
    follow-existing-conventions decisions from 05-CONTEXT.md.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && python .planning/phases/05-documentation/check_docstring.py one_way_anova python/polars_statistics/exprs/parametric.py && grep -q 'one_way_anova' docs/api/tests/parametric.md && grep -q '0.6.0' CHANGELOG.md && grep -B1 'one_way_anova' src/expressions/parametric.rs | grep -q '///' && echo TRACER_OK</automated>
  </verify>
  <done>one_way_anova has a params+example docstring, a parametric.md section, a `///` on its Rust wrapper, and a 0.6.0 CHANGELOG entry; the verify prints TRACER_OK.</done>
</task>

<task type="auto">
  <name>Task 2: Add a docstring-audit helper and prove the docs build is clean (DOCS-02 gate)</name>
  <files>.planning/phases/05-documentation/check_docstring.py, .planning/phases/05-documentation/check_docs_build.py</files>
  <read_first>
    - mkdocs.yml (nav — parametric.md is already listed under Statistical Tests > Parametric Tests, so no nav edit is needed for one_way_anova; the expansion plans WILL edit nav)
  </read_first>
  <action>
    Create two small reusable checker scripts under the phase dir that all Phase 5 plans use (this is
    the tracer establishing the shared validation the expansion plans reuse):
    (a) check_docstring.py — takes a symbol name and a python file path, parses the file with `ast`,
    finds the FunctionDef with that name, and exits non-zero unless its docstring contains both a
    "Parameters" section and at least one runnable `>>>` example line (this is the DOCS-01 gate the
    expansion plans call per-symbol).
    (b) check_docs_build.py — detects whether mkdocs is importable; if so, runs `mkdocs build --strict`
    into a throwaway site dir and exits non-zero on any broken reference; if mkdocs is NOT available in
    this environment (it is not on PATH at plan time), it falls back to a structural check that parses
    mkdocs.yml and asserts every nav-referenced markdown path exists under docs/. Do NOT install mkdocs
    and do NOT require a maturin rebuild. Then run check_docs_build.py to prove the tracer entry is
    clean, and record in the SUMMARY which path (real mkdocs build vs. fallback) was taken.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && python .planning/phases/05-documentation/check_docs_build.py</automated>
  </verify>
  <done>Both checker scripts exist; check_docs_build.py exits 0 (clean mkdocs build or clean nav-reference fallback).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only phase: no runtime code path, no untrusted input, no new attack surface. Docstrings, doc comments, markdown, and CHANGELOG text only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | docs build (mkdocs) | low | accept | No installs occur; build runs read-only against local markdown. No package-manager installs in this phase, so no package-legitimacy gate is required. |
| T-05-02 | Information Disclosure | runnable docstring examples | low | mitigate | Examples use only small inline DataFrames/arrays — no secrets, credentials, or external data per D (examples-must-be-runnable). |
</threat_model>

<verification>
- `python .planning/phases/05-documentation/check_docstring.py one_way_anova python/polars_statistics/exprs/parametric.py` exits 0
- one_way_anova present in docs/api/tests/parametric.md, in a `///` comment in src/expressions/parametric.rs, and in a 0.6.0 CHANGELOG section
- `python .planning/phases/05-documentation/check_docs_build.py` exits 0
</verification>

<success_criteria>
one_way_anova is documented on all four surfaces; the shared checker scripts exist; the docs build
(or fallback reference check) is clean. The doc pipeline is proven for the expansion plans.
</success_criteria>

<output>
Create `.planning/phases/05-documentation/05-01-SUMMARY.md` when done (record whether mkdocs ran a real build or the fallback was used).
</output>
