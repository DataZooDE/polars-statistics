---
phase: 05-documentation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - python/polars_statistics/exprs/parametric.py
  - python/polars_statistics/exprs/modern.py
  - python/polars_statistics/exprs/correlation.py
  - python/polars_statistics/exprs/regression.py
  - src/expressions/parametric.rs
  - src/expressions/modern.rs
  - src/expressions/correlation.rs
  - src/expressions/regression.rs
  - src/pymodels/py_gamma.rs
  - src/pymodels/py_glmm.rs
  - src/pymodels/py_pspline.rs
  - src/pymodels/py_theil_sen.rs
  - src/pymodels/py_ransac.rs
  - src/pymodels/py_bayesian_ridge.rs
  - src/pymodels/py_ard.rs
  - src/pymodels/py_lars.rs
  - src/pymodels/py_passive_aggressive.rs
  - src/pymodels/py_moment_accumulator.rs
  - src/pymodels/py_ols.rs
  - src/pymodels/py_ridge.rs
  - src/pymodels/py_wls.rs
autonomous: true
requirements: [DOCS-01, DOCS-03]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every new statistics expression (two_way_anova, repeated_measures_anova, energy_distance_nd, icc) has a docstring with signature, parameters, and a runnable example"
    - "Every new regression PyModel class and its methods have PyO3 /// docstrings (which serve as the Python class docstrings) with a runnable example"
    - "Every new *_fit Rust wrapper fn and the 5 gamma_* diagnostic expressions carry /// doc comments"
    - "The WLS hc_inference NotImplementedError limitation and the ICC contract change are noted in the relevant docstrings"
  artifacts:
    - src/pymodels/py_glmm.rs
    - python/polars_statistics/exprs/regression.py
  key_links:
    - "PyO3 /// class docstrings surface as the Python help() text for Gamma/GLMM/PSpline/etc."
---

<objective>
Audit-and-fill DOCS-01 (Python docstrings) and DOCS-03 (Rust `///` doc comments) for every remaining
newly exposed symbol from Phases 3 and 4 (all except one_way_anova, done in the 05-01 tracer). Many
symbols already carry docstrings from their implementation plans — this is an audit pass that fills
gaps and guarantees every one has a signature, documented parameters, and a RUNNABLE example.

Purpose: DOCS-01 + DOCS-03 across the full new surface. The new PyModel classes are Rust-backed, so a
single PyO3 `///` block per class satisfies both requirements for that class.
Output: Complete docstrings and doc comments for all new statistics + regression API, including the
WLS-HC NotImplementedError and ICC contract-change caveats (per 05-CONTEXT.md specifics).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/02-public-api-audit/02-API-AUDIT.md
@.planning/phases/05-documentation/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill Python docstrings for the remaining statistics expressions (DOCS-01)</name>
  <files>python/polars_statistics/exprs/parametric.py, python/polars_statistics/exprs/modern.py, python/polars_statistics/exprs/correlation.py</files>
  <read_first>
    - python/polars_statistics/exprs/parametric.py (two_way_anova near line 69, repeated_measures_anova near line 147)
    - python/polars_statistics/exprs/modern.py (energy_distance_nd near line 72)
    - python/polars_statistics/exprs/correlation.py (the rewritten icc builder — new matrix-input contract from Phase 3)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (STAT-02/03/04/05 rows; ICC "exposed-but-stubbed" Special-Case Flag = the new matrix-input contract)
    - python/polars_statistics/exprs/parametric.py one_way_anova docstring (the template the 05-01 tracer confirmed)
  </read_first>
  <action>
    For two_way_anova, repeated_measures_anova (parametric.py), energy_distance_nd (modern.py), and
    icc (correlation.py): ensure each docstring documents the signature, every parameter, and a RUNNABLE
    Examples block with `>>>` lines over a small inline pl.DataFrame — fill gaps, do not rewrite
    complete docstrings. For icc specifically, document the NEW matrix-input contract (n_raters + rater
    columns) and note it replaced the former all-NaN stub, per the 05-CONTEXT.md specific about the ICC
    contract change (DOCS-01). Cite STAT-02/03/04/05 coverage in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && python .planning/phases/05-documentation/check_docstring.py two_way_anova python/polars_statistics/exprs/parametric.py && python .planning/phases/05-documentation/check_docstring.py repeated_measures_anova python/polars_statistics/exprs/parametric.py && python .planning/phases/05-documentation/check_docstring.py energy_distance_nd python/polars_statistics/exprs/modern.py && python .planning/phases/05-documentation/check_docstring.py icc python/polars_statistics/exprs/correlation.py</automated>
  </verify>
  <done>All four remaining statistics expressions pass the docstring gate (Parameters + runnable example); icc docstring notes the matrix-input contract.</done>
</task>

<task type="auto">
  <name>Task 2: Audit/fill PyO3 /// docstrings on the 10 new regression PyModels + HC/accumulator methods (DOCS-01 + DOCS-03)</name>
  <files>src/pymodels/py_gamma.rs, src/pymodels/py_glmm.rs, src/pymodels/py_pspline.rs, src/pymodels/py_theil_sen.rs, src/pymodels/py_ransac.rs, src/pymodels/py_bayesian_ridge.rs, src/pymodels/py_ard.rs, src/pymodels/py_lars.rs, src/pymodels/py_passive_aggressive.rs, src/pymodels/py_moment_accumulator.rs, src/pymodels/py_ols.rs, src/pymodels/py_ridge.rs, src/pymodels/py_wls.rs</files>
  <read_first>
    - src/pymodels/py_gamma.rs (already has ~77 /// lines — the completeness template for the other classes)
    - src/pymodels/py_moment_accumulator.rs (already shows a >>> example in its /// block)
    - src/pymodels/py_ols.rs (hc_inference near line 371; fit_from_accumulator near line 507)
    - src/pymodels/py_ridge.rs (hc_inference near line 159; fit_from_accumulator near line 227)
    - src/pymodels/py_wls.rs (hc_inference near line 145 — this one RAISES NotImplementedError)
    - .planning/phases/02-public-api-audit/02-API-AUDIT.md (REGR-01..06 rows for the class list)
  </read_first>
  <action>
    For each of the 10 new PyModel classes (Gamma, GLMM, PSpline, TheilSen, RANSAC, BayesianRidge, ARD,
    LARS, PassiveAggressive, MomentAccumulator): ensure the `#[pyclass]` struct carries a `///` class
    doc block with a one-paragraph description AND a runnable `>>>` example (these PyO3 comments ARE the
    Python class docstrings visible via help(), so they satisfy DOCS-01 for the class and DOCS-03 for
    the wrapper simultaneously), and ensure each public `#[pymethods]` method (fit/predict/getters) has
    a `///` line. Use py_gamma.rs as the completeness bar; fill gaps only. Additionally: on
    py_ols.rs/py_ridge.rs add/complete `///` on `hc_inference` and `fit_from_accumulator`; on
    py_wls.rs, the `hc_inference` `///` MUST state that it raises NotImplementedError for WLS (the
    limitation from 05-CONTEXT.md specifics). Do NOT rebuild with maturin.
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && python .planning/phases/05-documentation/check_rust_docs.py</automated>
  </verify>
  <done>Every new PyModel struct and its public methods carry /// docs with a runnable example; py_wls hc_inference documents the NotImplementedError limitation; the checker exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Add /// doc comments to the new Rust expression wrapper fns (DOCS-03)</name>
  <files>src/expressions/parametric.rs, src/expressions/modern.rs, src/expressions/correlation.rs, src/expressions/regression.rs, .planning/phases/05-documentation/check_rust_docs.py</files>
  <read_first>
    - src/expressions/regression.rs (the 5 gamma diagnostic wrappers: gamma_dispersion_deviance_fit ~2350, gamma_pearson_chi_squared_fit ~2421, gamma_standardized_pearson_residuals_fit ~2459, gamma_standardized_deviance_residuals_fit ~2498, and gamma_dispersion_pearson_fit; the Ridge/WLS HC expression additions)
    - src/expressions/modern.rs (energy_distance_nd `*_fit` wrapper)
    - src/expressions/correlation.rs (icc_fit — rewritten matrix-input version)
    - src/expressions/parametric.rs (two_way_anova / repeated_measures_anova `*_fit` wrappers)
  </read_first>
  <action>
    First author check_rust_docs.py: a checker that greps each listed pymodels/*.rs and expressions/*.rs
    file and asserts a `///` line immediately precedes each named `pub fn *_fit`, each new `#[pyclass]`
    struct, and (for py_wls.rs) that its hc_inference doc mentions NotImplementedError. Then add or
    complete a `///` doc comment on each public `*_fit` wrapper for: two_way_anova, repeated_measures_anova
    (parametric.rs); energy_distance_nd (modern.rs); icc (correlation.rs); and the 5 gamma_* diagnostic
    expressions + the Ridge/WLS HC expression wrappers (regression.rs). Each `///` states what the
    expression computes and its output struct, matching existing style. Do NOT rebuild with maturin;
    correctness is verified by grep, not compilation (compilation is Phase 6's gate).
  </action>
  <verify>
    <automated>cd /home/simonm/projects/rust/polars-statistics && python .planning/phases/05-documentation/check_rust_docs.py && grep -B1 'gamma_dispersion_deviance_fit' src/expressions/regression.rs | grep -q '///' && grep -B1 'gamma_standardized_deviance_residuals_fit' src/expressions/regression.rs | grep -q '///'</automated>
  </verify>
  <done>check_rust_docs.py exits 0; the 5 gamma_* wrappers and the ANOVA/energy/icc wrappers all carry /// doc comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only: docstrings and `///` comments; no runtime code path or untrusted input added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | runnable docstring/`///` examples | low | mitigate | Examples use only small inline DataFrames/arrays; no secrets or external data. No package installs, so no legitimacy gate needed. |
</threat_model>

<verification>
- check_docstring.py passes for two_way_anova, repeated_measures_anova, energy_distance_nd, icc
- check_rust_docs.py exits 0 (all 10 new PyModels + new *_fit wrappers carry /// with examples where required)
- py_wls.rs hc_inference doc mentions NotImplementedError; icc docstring notes the matrix-input contract
</verification>

<success_criteria>
Every newly exposed symbol except the tracer's one_way_anova has a complete Python docstring
(signature + params + runnable example) and, for Rust-backed symbols, a `///` doc comment; the two
documented caveats (WLS-HC NotImplementedError, ICC contract change) are present.
</success_criteria>

<output>
Create `.planning/phases/05-documentation/05-02-SUMMARY.md` when done.
</output>
